<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="bader_inbox.InboxMain" owl="1">
        <div class="bader-inbox-container d-flex">
            <!-- LEFT PANEL: Conversations -->
            <div class="bader-inbox-left">
                <div class="panel-header-v2">
                    <!-- Top bar: Title + Actions -->
                    <div class="panel-top-bar">
                        <h3 class="panel-title">Conversaciones</h3>
                        <div class="panel-actions">
                            <!-- Channel Filter -->
                            <div class="toolbar-dropdown" t-if="state.channels.length">
                                <button t-attf-class="toolbar-btn {{ state.channelFilter ? 'active' : '' }}" 
                                        t-on-click="toggleChannelPopover" title="Filtrar por canal">
                                    <i class="fa fa-plug"/>
                                    <span t-if="state.channelFilter" class="toolbar-indicator" t-attf-style="background: {{ getChannelColor(state.channelFilter) }}"/>
                                </button>
                                <div t-if="state.showChannelPopover" class="toolbar-popover">
                                    <div class="popover-item" t-on-click="() => this.selectChannelFromPopover(null)">
                                        <i class="fa fa-globe popover-icon"/>
                                        <span>Todos los canales</span>
                                        <i t-if="!state.channelFilter" class="fa fa-check popover-check"/>
                                    </div>
                                    <t t-foreach="state.channels" t-as="ch" t-key="ch.id">
                                        <div class="popover-item" t-on-click="() => this.selectChannelFromPopover(ch.id)">
                                            <span class="popover-dot" t-attf-style="background: {{ getChannelColor(ch.id) }}"/>
                                            <span t-esc="ch.name"/>
                                            <span t-if="ch.state === 'connected'" class="popover-status connected">●</span>
                                            <span t-else="" class="popover-status disconnected">●</span>
                                            <i t-if="state.channelFilter === ch.id" class="fa fa-check popover-check"/>
                                        </div>
                                    </t>
                                </div>
                            </div>

                            <!-- Tag Filter -->
                            <div class="toolbar-dropdown" t-if="state.allTags.length">
                                <button t-attf-class="toolbar-btn {{ state.activeTagFilter ? 'active' : '' }}"
                                        t-on-click="toggleTagPopover" title="Filtrar por etiqueta">
                                    <i class="fa fa-tag"/>
                                    <span t-if="state.activeTagFilter" class="toolbar-indicator" t-attf-style="background: {{ getTagColor(state.activeTagFilter) }}"/>
                                </button>
                                <div t-if="state.showTagPopover" class="toolbar-popover">
                                    <div class="popover-item" t-on-click="() => this.selectTagFromPopover(null)">
                                        <i class="fa fa-tags popover-icon"/>
                                        <span>Todas las etiquetas</span>
                                        <i t-if="!state.activeTagFilter" class="fa fa-check popover-check"/>
                                    </div>
                                    <t t-foreach="state.allTags" t-as="tag" t-key="tag.id">
                                        <div class="popover-item" t-on-click="() => this.selectTagFromPopover(tag.id)">
                                            <span class="popover-dot" t-attf-style="background: {{ getTagColor(tag.id) }}"/>
                                            <span t-esc="tag.name"/>
                                            <i t-if="state.activeTagFilter === tag.id" class="fa fa-check popover-check"/>
                                        </div>
                                    </t>
                                </div>
                            </div>

                            <!-- Action Buttons -->
                            <button class="panel-action-btn" t-on-click="openAddChannelModal" title="Añadir Canal WhatsApp">
                                <i class="fa fa-plus"/>
                            </button>
                            <button class="panel-action-btn accent" t-on-click="openNewConversationModal" title="Nueva Conversación">
                                <i class="fa fa-comment"/>
                            </button>
                        </div>
                    </div>

                    <!-- Contacts / Groups Tab Bar -->
                    <div class="inbox-tab-bar">
                        <button t-attf-class="inbox-tab {{ state.activeTab === 'contacts' ? 'active' : '' }}"
                                t-on-click="() => this.switchTab('contacts')">
                            <i class="fa fa-user"/> Contactos
                        </button>
                        <button t-attf-class="inbox-tab {{ state.activeTab === 'groups' ? 'active' : '' }}"
                                t-on-click="() => this.switchTab('groups')">
                            <i class="fa fa-users"/> Grupos
                            <span t-if="groupUnreadCount" class="tab-badge" t-esc="groupUnreadCount"/>
                        </button>
                    </div>

                    <!-- Search Bar -->
                    <div class="panel-search">
                        <i class="fa fa-search panel-search-icon"/>
                        <input type="text" class="panel-search-input" placeholder="Buscar conversación..."
                               t-model="state.searchQuery" t-on-input="onSearchInput"/>
                        <button t-if="state.searchQuery" class="panel-search-clear" t-on-click="() => { this.state.searchQuery = ''; this.state.remoteSearchResults = []; }">
                            <i class="fa fa-times"/>
                        </button>
                    </div>

                    <!-- Active filter pills (only when filters active) -->
                    <div t-if="state.channelFilter || state.activeTagFilter" class="toolbar-active-filters">
                        <span t-if="state.channelFilter" class="active-filter-pill" t-on-click="() => this.selectChannelFromPopover(null)">
                            <span class="pill-dot" t-attf-style="background: {{ getChannelColor(state.channelFilter) }}"/>
                            <t t-esc="getChannelDisplayName(state.channelFilter)"/>
                            <i class="fa fa-times pill-close"/>
                        </span>
                        <span t-if="state.activeTagFilter" class="active-filter-pill" t-on-click="() => this.selectTagFromPopover(null)">
                            <span class="pill-dot" t-attf-style="background: {{ getTagColor(state.activeTagFilter) }}"/>
                            <t t-esc="getTagName(state.activeTagFilter)"/>
                            <i class="fa fa-times pill-close"/>
                        </span>
                    </div>
                </div>

                <!-- Split: Vertical Sidebar Filters + Conversations -->
                <div class="panel-body-split">
                    <!-- Vertical Filter Sidebar -->
                    <div class="filter-sidebar">
                        <button t-attf-class="filter-sidebar-btn {{ state.filter === 'all' ? 'active' : '' }}" 
                                t-on-click="() => this.setFilter('all')" title="Todas">
                            <i class="fa fa-comments"/>
                            <span class="filter-count" t-if="allCount" t-esc="allCount"/>
                        </button>
                        <button t-attf-class="filter-sidebar-btn {{ state.filter === 'unread' ? 'active' : '' }}" 
                                t-on-click="() => this.setFilter('unread')" title="No leídas">
                            <i class="fa fa-envelope"/>
                            <span t-if="totalUnread" class="filter-badge" t-esc="totalUnread"/>
                        </button>
                        <button t-attf-class="filter-sidebar-btn {{ state.filter === 'mine' ? 'active' : '' }}" 
                                t-on-click="() => this.setFilter('mine')" title="Mías">
                            <i class="fa fa-user"/>
                            <span class="filter-count" t-if="mineCount" t-esc="mineCount"/>
                        </button>
                        <button t-attf-class="filter-sidebar-btn {{ state.filter === 'shared' ? 'active' : '' }}" 
                                t-on-click="() => this.setFilter('shared')" title="Compartidas">
                            <i class="fa fa-share-alt"/>
                            <span t-if="sharedCount" class="filter-badge shared" t-esc="sharedCount"/>
                        </button>
                        <div class="filter-sidebar-spacer"/>
                        <button t-attf-class="filter-sidebar-btn closed {{ state.filter === 'closed' ? 'active' : '' }}" 
                                t-on-click="() => this.setFilter('closed')" title="Cerradas">
                            <i class="fa fa-archive"/>
                        </button>
                    </div>

                <div class="conversations-list">
                    <div t-if="state.loadingConversations" class="skeleton-loading">
                            <div class="skeleton-conversation" t-foreach="[1,2,3,4,5]" t-as="i" t-key="i">
                                <div class="skeleton-avatar"/>
                                <div class="skeleton-content">
                                    <div class="skeleton-line w-60"/>
                                    <div class="skeleton-line w-80"/>
                                </div>
                            </div>
                        </div>
                    <div t-elif="!filteredConversations.length" class="empty-state">
                            <i class="fa fa-inbox"/>
                            <p>Sin conversaciones</p>
                        </div>
                    <div t-else="" class="conversations-items">
                        <t t-foreach="filteredConversations" t-as="conv" t-key="conv.id">
                            <div t-attf-class="conversation-item {{ state.selectedConversation?.id === conv.id ? 'active' : '' }} {{ conv.unread_count > 0 ? 'unread' : '' }} {{ conv.is_pinned ? 'pinned' : '' }}" 
                                 t-on-click="() => this.selectConversation(conv)">
                                <!-- Channel color indicator -->
                                <div class="channel-indicator" t-attf-style="background: {{ getChannelColor(conv.channel_id) }}"
                                     t-att-title="getChannelDisplayName(conv.channel_id)"/>
                                <div t-attf-class="conv-avatar {{ conv.profile_pic_url ? '' : getAvatarColor(conv.id) }}">
                                    <img t-if="conv.profile_pic_url" t-att-src="conv.profile_pic_url" class="conv-avatar-img" alt="" loading="lazy"
                                         t-on-error="(ev) => { ev.target.style.display='none'; ev.target.parentNode.classList.add(this.getAvatarColor(conv.id)); ev.target.parentNode.insertAdjacentText('afterbegin', this.getInitials(conv.computed_name || conv.phone)); }"/>
                                    <t t-else="" t-esc="getInitials(conv.computed_name || conv.phone)"/>
                                    <div class="status-dot online"/>
                                </div>
                                <div class="conv-content">
                                    <div class="conv-header">
                                        <div class="conv-name-row">
                                            <i t-if="conv.is_pinned" class="fa fa-thumb-tack conv-pin-icon" title="Fijada"/>
                                            <i t-if="conv.is_group" class="fa fa-users conv-group-icon" title="Grupo"/>
                                            <span t-attf-class="conv-name {{ conv.unread_count > 0 ? 'fw-bold' : '' }}" t-esc="conv.computed_name || conv.phone"/>
                                            <span t-if="conv.partner_id" class="odoo-contact-badge" title="Contacto Odoo">✓</span>
                                            <i t-if="conv.is_muted" class="fa fa-bell-slash conv-muted-icon" title="Silenciado"/>
                                            <span t-if="conv.is_group and conv.member_count" class="conv-member-count" t-att-title="conv.member_count + ' miembros'"><t t-esc="conv.member_count"/> <i class="fa fa-user" style="font-size: 9px;"/></span>
                                        </div>
                                        <span t-attf-class="conv-time {{ conv.unread_count > 0 ? 'unread-time' : '' }}" t-esc="formatTime(conv.last_message_date)"/>
                                    </div>
                                    <div class="conv-preview">
                                        <span t-attf-class="conv-message {{ conv.unread_count > 0 ? 'unread-msg' : '' }}" t-esc="formatLastMessage(conv.last_message)"/>
                                        <div class="conv-preview-right">
                                            <!-- Assignee avatar -->
                                            <span t-if="conv.assigned_user_id" class="conv-assignee" 
                                                  t-att-title="conv.assigned_user_id[1]" 
                                                  t-esc="getInitials(conv.assigned_user_id[1])"/>
                                            <span t-if="conv.unread_count > 0" class="conv-badge" t-esc="conv.unread_count"/>
                                        </div>
                                    </div>
                                    <!-- Compact tags: dots only -->
                                    <div t-if="conv.tag_ids and conv.tag_ids.length" class="conv-tags-compact">
                                        <t t-foreach="conv.tag_ids" t-as="tagId" t-key="tagId">
                                            <span class="tag-dot" t-attf-style="background: {{ getTagColor(tagId) }}" 
                                                  t-att-title="getTagName(tagId)"/>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </div>
                </div>
                </div> <!-- /panel-body-split -->
            </div>

            <!-- CENTER PANEL: Chat or Kanban -->
            <div class="bader-inbox-center">
                <!-- View Mode Toggle -->
                <div class="view-mode-toggle">
                    <button t-attf-class="toggle-btn {{ state.viewMode === 'list' ? 'active' : '' }}" 
                            t-on-click="() => this.setViewMode('list')" title="Vista Chat">
                        <i class="fa fa-comments"/> Chat
                    </button>
                    <button t-attf-class="toggle-btn {{ state.viewMode === 'kanban' ? 'active' : '' }}" 
                            t-on-click="() => this.setViewMode('kanban')" title="Vista Kanban">
                        <i class="fa fa-columns"/> Kanban
                    </button>
                    <button t-attf-class="toggle-btn {{ state.viewMode === 'dashboard' ? 'active' : '' }}" 
                            t-on-click="() => this.setViewMode('dashboard')" title="Dashboard">
                        <i class="fa fa-bar-chart"/> Dashboard
                    </button>
                </div>

                <!-- ═══ LIST VIEW (Chat) ═══ -->
                <div t-if="state.viewMode === 'list'" class="list-view-container">
                    <div t-if="state.selectedConversation" class="chat-view"
                         t-on-dragenter="onDragEnterChat" t-on-dragover="onDragOverChat"
                         t-on-dragleave="onDragLeaveChat" t-on-drop="onDropChat">
                        <!-- Drag & Drop Overlay -->
                        <div t-if="state.isDragOver" class="drop-zone-overlay">
                            <div class="drop-zone-content">
                                <i class="fa fa-cloud-upload fa-3x"/>
                                <p>Suelta el archivo aquí</p>
                            </div>
                        </div>
                        <div class="chat-header">
                            <!-- F5: Mobile back button -->
                            <button class="mobile-back-btn" t-on-click="showMobileSidebar" title="Volver">
                                <i class="fa fa-arrow-left"/>
                            </button>
                            <div class="chat-contact">
                                <div t-attf-class="contact-avatar {{ state.selectedConversation.profile_pic_url ? '' : getAvatarColor(state.selectedConversation.id) }}">
                                    <img t-if="state.selectedConversation.profile_pic_url" t-att-src="state.selectedConversation.profile_pic_url" class="contact-avatar-img" alt=""
                                         t-on-error="(ev) => { ev.target.style.display='none'; ev.target.parentNode.classList.add(this.getAvatarColor(state.selectedConversation.id)); ev.target.parentNode.insertAdjacentText('afterbegin', this.getInitials(state.selectedConversation.computed_name || state.selectedConversation.phone)); }"/>
                                    <t t-else="" t-esc="getInitials(state.selectedConversation.computed_name || state.selectedConversation.phone)"/>
                                </div>
                                <div class="contact-info">
                                    <h4 t-esc="state.selectedConversation.computed_name || state.selectedConversation.phone"/>
                                    <p t-if="state.selectedConversation.is_group and state.selectedConversation.member_count">
                                        <i class="fa fa-users" style="margin-right: 4px;"/><t t-esc="state.selectedConversation.member_count"/> miembros
                                    </p>
                                    <p t-elif="state.selectedConversation.is_group and state.selectedConversation.group_description">
                                        <t t-esc="(state.selectedConversation.group_description || '').substring(0, 60)"/>
                                    </p>
                                    <p t-else="" t-esc="state.selectedConversation.phone"/>
                                </div>
                            </div>
                            <div class="chat-actions">
                                <button class="btn-chat-action" title="Buscar" t-on-click="toggleMessageSearch">
                                    <i class="fa fa-search"/>
                                </button>
                                <button class="btn-chat-action" title="Asignar" t-on-click="assignConversation">
                                    <i class="fa fa-user-plus"/>
                                </button>
                                <button class="btn-chat-action" title="Exportar CSV" t-on-click="exportConversation">
                                    <i class="fa fa-download"/>
                                </button>
                                <button t-if="state.selectedConversation.state !== 'resolved'" class="btn-chat-action" title="Resolver" t-on-click="resolveConversation">
                                    <i class="fa fa-check"/>
                                </button>
                                <button t-else="" class="btn-chat-action" title="Reabrir" t-on-click="reopenConversation" style="color: #f59e0b;">
                                    <i class="fa fa-undo"/>
                                </button>
                                <button t-attf-class="btn-chat-action btn-ai-toggle {{ state.selectedConversation.ai_active ? 'ai-on' : 'ai-off' }}"
                                        t-att-title="state.selectedConversation.ai_active ? 'AI Activo - Clic para desactivar' : 'AI Inactivo - Clic para activar'"
                                        t-on-click="toggleAI">
                                    <span style="font-size: 16px;">🤖</span>
                                </button>
                                <!-- Group-specific buttons -->
                                <button t-if="state.selectedConversation.is_group" class="btn-chat-action" 
                                        t-att-title="state.selectedConversation.is_muted ? 'Activar notificaciones' : 'Silenciar grupo'"
                                        t-on-click="toggleMute">
                                    <i t-attf-class="fa {{ state.selectedConversation.is_muted ? 'fa-bell-slash' : 'fa-bell' }}"/>
                                </button>
                                <button t-if="state.selectedConversation.is_group" class="btn-chat-action" title="Sincronizar grupo" t-on-click="syncGroupInfo">
                                    <i class="fa fa-refresh"/>
                                </button>
                                <button t-if="state.selectedConversation.is_group" class="btn-chat-action" title="Miembros" t-on-click="toggleGroupPanel"
                                        t-attf-class="btn-chat-action {{ state.showGroupPanel ? 'active' : '' }}">
                                    <i class="fa fa-users"/>
                                </button>
                                <button class="btn-chat-action" t-on-click="toggleContactPanel" t-att-title="state.showContactPanel ? 'Ocultar detalles' : 'Ver detalles'">
                                    <i class="fa fa-user"/>
                                </button>
                            </div>
                        </div>

                        <!-- Message Search Bar -->
                        <div t-if="state.showMessageSearch" class="message-search-bar">
                            <div class="search-input-wrapper">
                                <i class="fa fa-search"/>
                                <input type="text" placeholder="Buscar mensaje..." 
                                       t-model="state.messageSearchQuery"
                                       t-on-keydown="onSearchKeydown"/>
                                <span t-if="state.messageSearchResults.length" class="search-count">
                                    <t t-esc="state.messageSearchIndex + 1"/> de <t t-esc="state.messageSearchResults.length"/>
                                </span>
                            </div>
                            <div class="search-nav">
                                <button class="btn-search-nav" t-on-click="() => navigateSearchResult('prev')" t-att-disabled="state.messageSearchIndex === 0">
                                    <i class="fa fa-chevron-up"/>
                                </button>
                                <button class="btn-search-nav" t-on-click="() => navigateSearchResult('next')" t-att-disabled="state.messageSearchIndex >= state.messageSearchResults.length - 1">
                                    <i class="fa fa-chevron-down"/>
                                </button>
                                <button class="btn-search-nav" t-on-click="toggleMessageSearch">
                                    <i class="fa fa-times"/>
                                </button>
                            </div>
                        </div>

                        <!-- ═══ GROUP MEMBERS PANEL ═══ -->
                        <div t-if="state.showGroupPanel and state.selectedConversation and state.selectedConversation.is_group" class="group-members-panel">
                            <div class="gmp-header">
                                <h5><i class="fa fa-users"/> Miembros <span class="gmp-count" t-if="state.groupMembers.length" t-esc="state.groupMembers.length"/></h5>
                                <button class="gmp-close" t-on-click="toggleGroupPanel"><i class="fa fa-times"/></button>
                            </div>
                            <!-- Group Description -->
                            <div t-if="state.selectedConversation.group_description" class="gmp-description">
                                <p t-esc="state.selectedConversation.group_description"/>
                            </div>
                            <!-- Member Search -->
                            <div class="gmp-search">
                                <i class="fa fa-search"/>
                                <input type="text" placeholder="Buscar miembro..." t-model="state.groupMemberFilter"/>
                            </div>
                            <!-- Group message search -->
                            <div class="gmp-msg-search">
                                <div class="gmp-search">
                                    <i class="fa fa-comment"/>
                                    <input type="text" placeholder="Buscar en mensajes..." t-model="state.groupSearchQuery"
                                           t-on-keydown="(ev) => { ev.key === 'Enter' &amp;&amp; this.searchGroupMessages(); }"/>
                                </div>
                                <select class="gmp-participant-filter" t-model="state.participantFilter" t-on-change="searchGroupMessages">
                                    <option value="">Todos los participantes</option>
                                    <t t-foreach="state.groupMembers" t-as="m" t-key="m.id">
                                        <option t-att-value="m.phone" t-esc="m.display_name || m.phone"/>
                                    </t>
                                </select>
                                <!-- Search Results -->
                                <div t-if="state.groupSearchResults.length" class="gmp-search-results">
                                    <t t-foreach="state.groupSearchResults" t-as="r" t-key="r.id">
                                        <div class="gmp-search-result">
                                            <span class="gmp-sr-sender" t-esc="r.sender_name || 'Tú'"/>
                                            <span class="gmp-sr-content" t-esc="(r.content || '').substring(0, 80)"/>
                                            <span class="gmp-sr-time" t-esc="formatTime(r.create_date)"/>
                                        </div>
                                    </t>
                                </div>
                            </div>
                            <!-- Member List -->
                            <div class="gmp-list">
                                <t t-foreach="filteredGroupMembers" t-as="member" t-key="member.id">
                                    <div class="gmp-member" t-on-click="() => this.openMemberConversation(member)">
                                        <div t-attf-class="gmp-avatar {{ getAvatarColor(member.id) }}">
                                            <t t-esc="getInitials(member.display_name || member.phone)"/>
                                        </div>
                                        <div class="gmp-info">
                                            <span class="gmp-name" t-esc="member.display_name || member.phone"/>
                                            <span t-if="member.phone and member.name" class="gmp-phone" t-esc="member.phone"/>
                                        </div>
                                        <div class="gmp-badges">
                                            <span t-if="member.is_superadmin" class="gmp-badge superadmin" title="Super Admin">👑</span>
                                            <span t-elif="member.is_admin" class="gmp-badge admin" title="Admin">⭐</span>
                                        </div>
                                    </div>
                                </t>
                                <div t-if="!state.groupMembers.length" class="gmp-empty">
                                    <i class="fa fa-users"/>
                                    <p>Sin miembros sincronizados</p>
                                    <button class="btn btn-sm btn-primary" t-on-click="syncGroupInfo">
                                        <i class="fa fa-refresh"/> Sincronizar
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div class="chat-messages" t-ref="messagesContainer">
                            <div t-if="state.loadingMessages" class="skeleton-messages">
                                <div class="skeleton-msg skeleton-incoming"><div class="skeleton-msg-line" style="width:70%"/><div class="skeleton-msg-line" style="width:50%"/></div>
                                <div class="skeleton-msg skeleton-outgoing"><div class="skeleton-msg-line" style="width:60%"/><div class="skeleton-msg-line" style="width:40%"/></div>
                                <div class="skeleton-msg skeleton-incoming"><div class="skeleton-msg-line" style="width:80%"/><div class="skeleton-msg-line" style="width:45%"/></div>
                            </div>
                            <div t-else="" class="messages-list">
                                <t t-foreach="state.messages" t-as="msg" t-key="msg.id">
                                    <!-- Date separator (Hoy, Ayer, date) -->
                                    <div t-if="shouldShowDateSeparator(msg_index)" class="date-separator">
                                        <span t-esc="getDateLabel(msg.create_date)"/>
                                    </div>
                                    <!-- Internal Note (from @mention) -->
                                    <t t-if="msg._type === 'note'">
                                        <div class="internal-note">
                                            <div class="note-bubble">
                                                <div class="note-header">
                                                    <i class="fa fa-sticky-note-o"/> Nota interna
                                                    <span class="note-author" t-if="msg.author_id">— <t t-esc="msg.author_id[1]"/></span>
                                                </div>
                                                <div class="note-content" t-esc="msg.content"/>
                                                <div class="note-footer">
                                                    <span class="note-time" t-esc="msg.create_date"/>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                    <!-- Regular message -->
                                    <t t-else="">
                                    <div t-attf-class="message {{ msg.direction === 'out' ? 'outgoing' : 'incoming' }}{{ state.currentConversation and state.currentConversation.is_group and msg.direction === 'in' ? ' group-msg-row' : '' }}" t-att-data-msg-id="msg.id">
                                        <!-- Group sender avatar -->
                                        <div t-if="state.currentConversation and state.currentConversation.is_group and msg.direction === 'in' and msg.sender_name"
                                             class="group-sender-avatar"
                                             t-att-style="'background-color:' + getParticipantColor(msg.sender_name)"
                                             t-att-title="(msg.sender_name || '') + (msg.sender_phone ? ' (' + msg.sender_phone + ')' : '')">
                                            <t t-esc="(msg.sender_name || '?')[0].toUpperCase()"/>
                                        </div>
                                        <div class="message-bubble">
                                            <!-- Group sender name -->
                                            <div t-if="state.currentConversation and state.currentConversation.is_group and msg.direction === 'in' and msg.sender_name"
                                                 class="group-sender-name"
                                                 t-att-style="'color:' + getParticipantColor(msg.sender_name)">
                                                <t t-esc="msg.sender_name"/>
                                            </div>
                                            <!-- Quoted/Reply context -->
                                            <div t-if="msg.quoted_text" class="quoted-message">
                                                <div t-if="msg.quoted_participant" class="quoted-sender" t-esc="msg.quoted_participant"/>
                                                <div class="quoted-text" t-esc="msg.quoted_text"/>
                                            </div>
                                            <!-- Media content based on message type -->
                                            <div class="message-content-wrapper">
                                                <div t-if="msg.message_type === 'image'" class="message-media">
                                                    <img t-att-src="getMediaUrl(msg)" class="message-image" 
                                                         t-on-click="() => window.open(getMediaUrl(msg), '_blank')"
                                                         alt="Image" loading="lazy"
                                                         t-on-error="(ev) => this.onImageError(ev, msg)"/>
                                                </div>
                                                <div t-if="msg.message_type === 'audio'" class="message-media">
                                                    <audio controls="" preload="none">
                                                        <source t-att-src="getMediaUrl(msg)" t-att-type="msg.media_mimetype || 'audio/ogg'"/>
                                                    </audio>
                                                </div>
                                                <div t-if="msg.message_type === 'video'" class="message-media">
                                                    <video controls="" preload="none" class="message-video">
                                                        <source t-att-src="getMediaUrl(msg)" t-att-type="msg.media_mimetype || 'video/mp4'"/>
                                                    </video>
                                                </div>
                                                <div t-if="msg.message_type === 'document'" class="message-document">
                                                    <a t-att-href="getMediaUrl(msg)" target="_blank" class="document-link">
                                                        <i class="fa fa-file-o"/>
                                                        <span t-esc="msg.media_filename || 'Document'"/>
                                                    </a>
                                                </div>
                                                <div t-if="msg.message_type === 'sticker'" class="message-sticker"
                                                     t-att-data-sticker-url="getMediaUrl(msg)"
                                                     t-att-data-msg-id="msg.id">
                                                    <img t-att-src="getMediaUrl(msg)" class="sticker-image"
                                                         t-on-click="() => window.open(getMediaUrl(msg), '_blank')"
                                                         alt="Sticker" loading="lazy"
                                                         t-on-error="(ev) => this.loadLottieSticker(ev, msg)"/>
                                                </div>
                                                <div t-if="msg.message_type === 'location'" class="message-location">
                                                    <i class="fa fa-map-marker"/>
                                                    <span t-esc="msg.content || 'Location'"/>
                                                </div>
                                                <!-- Contact Card -->
                                                <div t-if="msg.message_type === 'contact'" class="message-contact-card">
                                                    <t t-set="contactData" t-value="parseContactCard(msg)"/>
                                                    <t t-if="contactData and contactData.contacts">
                                                        <!-- Multiple contacts -->
                                                        <div t-foreach="contactData.contacts" t-as="c" t-key="c_index" class="contact-card-item">
                                                            <div class="contact-card-avatar">👤</div>
                                                            <div class="contact-card-info">
                                                                <div class="contact-card-name" t-esc="c.displayName"/>
                                                                <div t-if="c.phone" class="contact-card-phone">
                                                                    <i class="fa fa-phone"/> <span t-esc="c.phone"/>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </t>
                                                    <t t-elif="contactData">
                                                        <!-- Single contact -->
                                                        <div class="contact-card-item">
                                                            <div class="contact-card-avatar">👤</div>
                                                            <div class="contact-card-info">
                                                                <div class="contact-card-name" t-esc="contactData.displayName"/>
                                                                <div t-if="contactData.phone" class="contact-card-phone">
                                                                    <i class="fa fa-phone"/> <span t-esc="contactData.phone"/>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </t>
                                                </div>
                                                <div t-if="msg.content and msg.message_type !== 'location' and msg.message_type !== 'contact'" class="message-text" t-out="formatWhatsAppText(msg)"/>
                                                <!-- Link Preview Card -->
                                                <t t-set="preview" t-value="parseLinkPreview(msg)"/>
                                                <a t-if="preview" t-att-href="preview.url" target="_blank" class="link-preview-card">
                                                    <img t-if="preview.image" t-att-src="preview.image" class="link-preview-img" loading="lazy"/>
                                                    <div class="link-preview-info">
                                                        <div class="link-preview-title" t-esc="preview.title"/>
                                                        <div t-if="preview.description" class="link-preview-desc" t-esc="preview.description"/>
                                                        <div class="link-preview-domain" t-esc="preview.domain"/>
                                                    </div>
                                                </a>
                                                <!-- Translation -->
                                                <div t-if="state.translations['show_' + msg.id] and msg.translated_content" class="translated-text">
                                                    <i class="fa fa-language"/> <t t-esc="msg.translated_content"/>
                                                </div>
                                            </div>
                                            <div class="message-meta">
                                                <!-- Language badge -->
                                                <span t-if="msg.detected_language and msg.direction === 'in'" class="lang-badge" t-esc="msg.detected_language.toUpperCase()"/>
                                                <!-- Translate button -->
                                                <button t-if="msg.direction === 'in' and msg.content and (state.aiEnabled or state.translationEnabled)" class="translate-btn" t-on-click="() => this.translateMessage(msg.id)"
                                                        t-att-title="state.translations['show_' + msg.id] ? 'Ocultar traducción' : 'Traducir'">
                                                    <i t-attf-class="fa {{ state.translations['translating_' + msg.id] ? 'fa-spinner fa-spin' : 'fa-language' }}"/>
                                                </button>
                                                <span t-if="msg.direction === 'out' and msg.author_id" class="agent-inline" t-esc="getAgentShortName(msg)"/>
                                                <span t-if="msg.direction === 'out' and !msg.author_id" class="agent-inline mobile">📱</span>
                                                <span class="message-time" t-esc="formatTime(msg.create_date)"/>
                                                <span t-if="msg.is_edited" class="message-edited"><i class="fa fa-pencil"/> editado</span>
                                                <span t-if="msg.direction === 'out'" t-attf-class="message-status {{ msg.status === 'read' ? 'read' : msg.status === 'delivered' ? 'delivered' : msg.status === 'failed' ? 'failed' : 'sent' }}">
                                                    <span t-if="msg.status === 'failed'"><i class="fa fa-times"/></span>
                                                    <span t-elif="msg.status === 'delivered' or msg.status === 'read'"><i class="fa fa-check"/><i class="fa fa-check"/></span>
                                                    <span t-else=""><i class="fa fa-check"/></span>
                                                </span>
                                                <span t-else="" class="message-status-placeholder"/>
                                            </div>
                                            <!-- F10/F11/F17: Message Actions Bar -->
                                            <div class="msg-actions-bar">
                                                <button class="msg-action-btn" title="Responder" t-on-click="() => this.setReplyTo(msg)">
                                                    <i class="fa fa-reply"/>
                                                </button>
                                                <button class="msg-action-btn" title="Reaccionar" t-on-click="() => this.toggleReactionPicker(msg.id)">
                                                    <span>😊</span>
                                                </button>
                                                <button t-if="msg.direction === 'in' and msg.content and (state.aiEnabled or state.translationEnabled)" 
                                                        class="msg-action-btn msg-action-translate" 
                                                        t-on-click="() => this.translateMessage(msg.id)"
                                                        t-att-title="state.translations['show_' + msg.id] ? 'Ocultar traducción' : 'Traducir a Español 🇪🇸'"
                                                        t-att-disabled="state.translations['translating_' + msg.id]">
                                                    <i t-attf-class="fa {{ state.translations['translating_' + msg.id] ? 'fa-spinner fa-spin' : 'fa-language' }}"/>
                                                </button>
                                                <button class="msg-action-btn" title="Reenviar" t-on-click="() => this.forwardMessage(msg)">
                                                    <i class="fa fa-share"/>
                                                </button>
                                            </div>
                                            <!-- F11: Reaction display -->
                                            <div t-if="msg.reactions" class="msg-reactions">
                                                <t t-foreach="msg.reactions" t-as="reaction" t-key="reaction">
                                                    <span class="msg-reaction-badge" t-esc="reaction"/>
                                                </t>
                                            </div>
                                        </div>
                                    </div>
                                    </t>
                                </t>
                            </div>
                        </div>

                        <div class="chat-composer">
                            <!-- F10: Reply Preview -->
                            <div t-if="state.replyingTo" class="reply-preview">
                                <div class="reply-preview-content">
                                    <span class="reply-preview-name" t-esc="state.replyingTo.direction === 'in' ? (state.selectedConversation.computed_name || state.selectedConversation.phone) : 'Tú'"/>
                                    <span class="reply-preview-text" t-esc="(state.replyingTo.content || state.replyingTo.message_type || '').substring(0, 80)"/>
                                </div>
                                <button class="reply-preview-close" t-on-click="() => this.state.replyingTo = null">
                                    <i class="fa fa-times"/>
                                </button>
                            </div>
                            <!-- AI Suggestions -->
                            <div t-if="state.aiSuggestions.length" class="ai-suggestions">
                                <span class="ai-label">✨ Sugerencias AI</span>
                                <div class="ai-chips">
                                    <button t-foreach="state.aiSuggestions" t-as="suggestion" t-key="suggestion_index"
                                            class="ai-chip" t-on-click="() => this.useAISuggestion(suggestion)">
                                        <t t-esc="suggestion"/>
                                    </button>
                                </div>
                            </div>
                            <!-- Attachment Preview -->
                            <div t-if="state.attachment" class="attachment-preview">
                                <div class="preview-content">
                                    <img t-if="state.attachment.type === 'image'" t-att-src="state.attachment.preview" class="preview-image"/>
                                    <div t-else="" class="preview-file">
                                        <i t-attf-class="fa {{ state.attachment.type === 'audio' ? 'fa-music' : state.attachment.type === 'video' ? 'fa-video-camera' : 'fa-file' }}"/>
                                    </div>
                                    <span class="preview-name" t-esc="state.attachment.name"/>
                                </div>
                                <button class="btn-remove-attachment" t-on-click="removeAttachment">
                                    <i class="fa fa-times"/>
                                </button>
                            </div>

                            <!-- Quick Reply Picker -->
                            <div t-if="state.showQuickReplies" class="quick-reply-picker">
                                <div class="qr-header">
                                    <i class="fa fa-bolt"/>
                                    <span>Respuestas Rápidas</span>
                                </div>
                                <div class="qr-list">
                                    <t t-foreach="filteredQuickReplies()" t-as="qr" t-key="qr.id">
                                        <div t-attf-class="qr-item {{ qr_index === state.quickReplyIndex ? 'active' : '' }}"
                                             t-on-click="() => this.selectQuickReply(qr)">
                                            <div class="qr-shortcut">
                                                <span t-if="qr.shortcut" class="qr-slash" t-esc="qr.shortcut"/>
                                                <span class="qr-name" t-esc="qr.name"/>
                                            </div>
                                            <div class="qr-preview" t-esc="qr.content.substring(0, 80) + (qr.content.length > 80 ? '...' : '')"/>
                                        </div>
                                    </t>
                                    <div t-if="!filteredQuickReplies().length" class="qr-empty">
                                        <i class="fa fa-info-circle"/>
                                        No se encontraron respuestas rápidas
                                    </div>
                                </div>
                                <div class="qr-vars-hint">
                                    <i class="fa fa-code"/> Variables: <code>{nombre}</code> <code>{telefono}</code> <code>{agente}</code> <code>{crm}</code> <code>{presupuesto}</code>
                                </div>
                            </div>

                            <!-- Variable Autocomplete Picker -->
                            <div t-if="state.showVarPicker" class="var-picker">
                                <div class="var-picker-header">
                                    <i class="fa fa-code"/>
                                    <span>Insertar Variable</span>
                                </div>
                                <div class="var-picker-list">
                                    <t t-foreach="_filteredVarOptions()" t-as="v" t-key="v.key">
                                        <div t-attf-class="var-item {{ v_index === state.varIndex ? 'active' : '' }}"
                                             t-on-click="() => this._insertVar(v)">
                                            <i t-attf-class="fa {{ v.icon }} var-icon"/>
                                            <span class="var-label" t-esc="v.label"/>
                                            <span class="var-desc" t-esc="v.desc"/>
                                        </div>
                                    </t>
                                    <div t-if="!_filteredVarOptions().length" class="var-empty">
                                        <i class="fa fa-info-circle"/>
                                        No se encontró la variable
                                    </div>
                                </div>
                            </div>

                            <!-- Emoji Picker -->
                            <div t-if="state.showEmojiPicker" class="emoji-picker">
                                <div class="emoji-tabs">
                                    <t t-foreach="getEmojiCategories()" t-as="cat" t-key="cat.id">
                                        <button t-attf-class="emoji-tab {{ state.emojiCategory === cat.id ? 'active' : '' }}"
                                                t-on-click="() => { this.state.emojiCategory = cat.id; }"
                                                t-att-title="cat.label">
                                            <t t-esc="cat.icon"/>
                                        </button>
                                    </t>
                                </div>
                                <div class="emoji-grid">
                                    <t t-foreach="getEmojisForCategory(state.emojiCategory)" t-as="emoji" t-key="emoji_index">
                                        <button class="emoji-btn" t-on-click="() => this.onEmojiSelected(emoji)">
                                            <t t-esc="emoji"/>
                                        </button>
                                    </t>
                                </div>
                            </div>

                            <div class="composer-actions">
                                <button class="btn-composer" title="Emoji" t-on-click="toggleEmojiPicker">
                                    <i class="fa fa-smile-o"/>
                                </button>
                                <button class="btn-composer" title="Adjuntar archivo" t-on-click="triggerFileInput">
                                    <i class="fa fa-paperclip"/>
                                </button>
                                <button t-if="state.aiEnabled" class="btn-composer btn-ai" title="Sugerencias AI" t-on-click="getAISuggestions"
                                        t-att-disabled="state.loadingAI">
                                    <i t-attf-class="fa {{ state.loadingAI ? 'fa-spinner fa-spin' : 'fa-magic' }}"/>
                                </button>
                                <button class="btn-composer btn-catalog" title="Enviar producto" t-on-click="toggleProductCatalog">
                                    <i class="fa fa-shopping-bag"/>
                                </button>
                                <input type="file" t-ref="fileInput" class="d-none" t-on-change="onFileSelected"/>
                            </div>
                            <div class="composer-input">
                                <!-- @Mention Dropdown -->
                                <div t-if="state.showMentionDropdown" class="mention-dropdown">
                                    <div class="mention-dropdown-header">
                                        <i class="fa fa-at"/> Mencionar usuario
                                    </div>
                                    <t t-foreach="state.mentionUsers" t-as="user" t-key="user.id">
                                        <div t-attf-class="mention-item {{ state.mentionCursor === user_index ? 'active' : '' }}"
                                             t-on-click="() => this.selectMentionUser(user)">
                                            <div class="mention-avatar">
                                                <img t-if="user.has_avatar" t-attf-src="/web/image/res.users/{{ user.id }}/image_128" alt=""/>
                                                <span t-else="" class="mention-initial" t-esc="(user.name || '?')[0].toUpperCase()"/>
                                            </div>
                                            <div class="mention-info">
                                                <span class="mention-name" t-esc="user.name"/>
                                                <span class="mention-login" t-esc="user.login"/>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                                <!-- Mentioned Users Pills -->
                                <div t-if="state.mentionedUsers.length > 0" class="mention-pills">
                                    <span class="mention-pills-label">
                                        <i class="fa fa-sticky-note-o"/> Nota interna para:
                                    </span>
                                    <t t-foreach="state.mentionedUsers" t-as="mu" t-key="mu.id">
                                        <span class="mention-pill">
                                            @<t t-esc="mu.name"/>
                                            <i class="fa fa-times mention-pill-remove" t-on-click="() => this.removeMentionUser(mu.id)"/>
                                        </span>
                                    </t>
                                </div>
                                <!-- F9: Voice Recording Indicator -->
                                <div t-if="state.isRecording" class="voice-recording-indicator">
                                    <button class="recording-cancel-btn" t-on-click="cancelVoiceRecording" title="Cancelar">
                                        <i class="fa fa-trash"/>
                                    </button>
                                    <div class="waveform-container">
                                        <t t-foreach="state.waveformBars || []" t-as="barH" t-key="barH_index">
                                            <div class="waveform-bar" t-attf-style="height: {{ barH }}px"/>
                                        </t>
                                    </div>
                                    <span class="recording-timer" t-esc="Math.floor(state.recordingTime / 60) + ':' + ('' + (state.recordingTime % 60)).padStart(2, '0')"/>
                                    <span class="recording-dot"/>
                                </div>
                                <textarea t-else="" rows="1" placeholder="Escribe un mensaje... (/ para respuestas rápidas)" 
                                    t-ref="composerTextarea"
                                    t-model="state.composerText" 
                                    t-on-keydown="onComposerKeydown"
                                    t-on-input="onComposerInput"/>
                            </div>
                            <div class="send-group">
                                <!-- F9: Voice/Send toggle -->
                                <t t-if="state.isRecording">
                                    <button class="btn-send" t-on-click="stopVoiceRecording" title="Enviar audio">
                                        <i class="fa fa-paper-plane"/>
                                    </button>
                                </t>
                                <t t-else="">
                                    <button class="btn-send" t-on-click="sendMessage" t-att-disabled="state.sendingMessage || (!state.composerText.trim() and !state.attachment)">
                                        <i t-attf-class="fa {{ state.sendingMessage ? 'fa-spinner fa-spin' : 'fa-paper-plane' }}"/>
                                    </button>
                                    <button t-attf-class="voice-recorder-btn {{ state.isRecording ? 'recording' : '' }}" 
                                            t-on-click="startVoiceRecording" title="Grabar audio">
                                        <i class="fa fa-microphone"/>
                                    </button>
                                    <button class="btn-schedule" t-on-click="toggleScheduleModal" title="Programar mensaje"
                                            t-att-disabled="!state.composerText.trim()">
                                        <i class="fa fa-clock-o"/>
                                    </button>
                                    <div t-if="state.translationEnabled or state.aiEnabled" class="lang-picker-wrapper">
                                        <button class="btn-translate-composer" 
                                                t-on-click="toggleLangPicker" 
                                                title="Traducir a otro idioma"
                                                t-att-disabled="!state.composerText.trim() || state.translatingComposer">
                                            <i t-attf-class="fa {{ state.translatingComposer ? 'fa-spinner fa-spin' : 'fa-language' }}"/>
                                            <span t-if="state.composerTargetLang" class="lang-badge-mini" t-esc="state.composerTargetLang.toUpperCase()"/>
                                        </button>
                                        <div t-if="state.showLangPicker" class="lang-picker-dropdown">
                                            <div class="lang-picker-header">🌐 Traducir a...</div>
                                            <t t-foreach="translationLanguages" t-as="lang" t-key="lang.code">
                                                <button class="lang-picker-item" 
                                                        t-on-click="() => this.selectComposerLang(lang.code)"
                                                        t-attf-class="lang-picker-item {{ lang.detected ? 'detected' : '' }}">
                                                    <span class="lang-flag" t-esc="lang.flag"/>
                                                    <span class="lang-name" t-esc="lang.name"/>
                                                    <span t-if="lang.detected" class="lang-detected-badge">✨ detectado</span>
                                                </button>
                                            </t>
                                        </div>
                                    </div>
                                </t>
                            </div>

                            <!-- Product Catalog Modal -->
                            <div t-if="state.showProductCatalog" class="product-catalog-modal">
                                <div class="catalog-overlay" t-on-click="toggleProductCatalog"/>
                                <div class="catalog-panel">
                                    <div class="catalog-header">
                                        <div class="catalog-title">
                                            <i class="fa fa-shopping-bag"/> Catálogo de Productos
                                        </div>
                                        <button class="catalog-close" t-on-click="toggleProductCatalog">
                                            <i class="fa fa-times"/>
                                        </button>
                                    </div>
                                    <div class="catalog-search">
                                        <div class="catalog-search-wrap">
                                            <i class="fa fa-search"/>
                                            <input type="text" placeholder="Buscar por nombre o SKU..."
                                                   t-att-value="state.productSearchQuery"
                                                   t-on-input="onCatalogSearchInput"
                                                   t-on-keydown="onCatalogSearchKeydown"/>
                                            <i t-if="state.productSearching" class="fa fa-spinner fa-spin search-spinner"/>
                                        </div>
                                    </div>
                                    <div class="catalog-results">
                                        <t t-if="state.productResults.length">
                                            <t t-foreach="state.productResults" t-as="prod" t-key="prod.id">
                                                <div class="product-card">
                                                    <div class="product-card-img">
                                                        <img t-if="prod.image" t-att-src="'data:image/png;base64,' + prod.image" t-att-alt="prod.name"/>
                                                        <div t-else="" class="product-no-img">
                                                            <i class="fa fa-camera"/>
                                                        </div>
                                                        <span t-if="prod.offer" class="product-badge-offer">OFERTA</span>
                                                        <span t-if="prod.stock" class="product-badge-stock">En stock</span>
                                                    </div>
                                                    <div class="product-card-info">
                                                        <div class="product-name" t-esc="prod.name"/>
                                                        <div t-if="prod.ref" class="product-sku">SKU: <t t-esc="prod.ref"/></div>
                                                        <div class="product-prices">
                                                            <t t-if="prod.offer">
                                                                <span class="price-old"><t t-esc="prod.currency"/><t t-esc="prod.pvp.toFixed(2)"/></span>
                                                                <span class="price-offer"><t t-esc="prod.currency"/><t t-esc="prod.offer.toFixed(2)"/></span>
                                                            </t>
                                                            <t t-else="">
                                                                <span class="price-current"><t t-esc="prod.currency"/><t t-esc="prod.pvp.toFixed(2)"/></span>
                                                            </t>
                                                        </div>
                                                    </div>
                                                    <button class="product-send-btn" t-on-click="() =&gt; this.sendProductCard(prod)"
                                                            t-att-disabled="state.sendingProduct === prod.id">
                                                        <i t-attf-class="fa {{ state.sendingProduct === prod.id ? 'fa-spinner fa-spin' : 'fa-paper-plane' }}"/>
                                                    </button>
                                                </div>
                                            </t>
                                        </t>
                                        <div t-elif="state.productSearchQuery.length &gt;= 2 and !state.productSearching" class="catalog-empty">
                                            <i class="fa fa-search"/>
                                            <span>No se encontraron productos</span>
                                        </div>
                                        <div t-else="" class="catalog-empty">
                                            <i class="fa fa-shopping-bag"/>
                                            <span>Escribe para buscar productos</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Schedule Message Modal -->
                            <div t-if="state.showScheduleModal" class="schedule-modal">
                                <div class="schedule-modal-content">
                                    <h5><i class="fa fa-clock-o"/> Programar Mensaje</h5>
                                    <div class="schedule-fields">
                                        <input type="date" class="form-control form-control-sm" t-model="state.scheduleDate"/>
                                        <input type="time" class="form-control form-control-sm" t-model="state.scheduleTime"/>
                                    </div>
                                    <div class="schedule-actions">
                                        <button class="btn btn-sm btn-outline-secondary" t-on-click="toggleScheduleModal">Cancelar</button>
                                        <button class="btn btn-sm btn-success" t-on-click="scheduleMessage">
                                            <i class="fa fa-clock-o"/> Programar
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div t-else="" class="empty-chat">
                        <div class="empty-icon">
                            <i class="fa fa-comments"/>
                        </div>
                        <h3>Bader Inbox</h3>
                        <p>Selecciona una conversación para comenzar</p>
                        <div t-if="!state.channels.length" class="connect-cta">
                            <button class="btn btn-success btn-lg mt-3" t-on-click="openAddChannelModal">
                                <i class="fa fa-whatsapp"/> Conectar WhatsApp
                            </button>
                        </div>
                    </div>
                </div>

                <!-- ═══ DASHBOARD VIEW ═══ -->
                <div t-if="state.viewMode === 'dashboard'" class="dashboard-view">
                    <div t-if="state.loadingDashboard" class="text-center py-5">
                        <i class="fa fa-spinner fa-spin fa-3x"/>
                        <p class="mt-2">Cargando datos...</p>
                    </div>
                    <div t-elif="state.dashboardData" class="dashboard-content">
                        <h3 class="dashboard-title"><i class="fa fa-bar-chart"/> Métricas del Inbox</h3>

                        <!-- KPI Cards -->
                        <div class="kpi-grid">
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(239,68,68,0.15); color: #EF4444"><i class="fa fa-comments"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value" t-esc="state.dashboardData.open_count"/>
                                    <span class="kpi-label">Conversaciones Abiertas</span>
                                </div>
                            </div>
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(16,185,129,0.15); color: #10B981"><i class="fa fa-check-circle"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value" t-esc="state.dashboardData.resolved_today"/>
                                    <span class="kpi-label">Resueltas Hoy</span>
                                </div>
                            </div>
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(59,130,246,0.15); color: #3B82F6"><i class="fa fa-paper-plane"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value" t-esc="state.dashboardData.sent_today"/>
                                    <span class="kpi-label">Enviadas Hoy</span>
                                </div>
                            </div>
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(245,158,11,0.15); color: #F59E0B"><i class="fa fa-inbox"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value" t-esc="state.dashboardData.received_today"/>
                                    <span class="kpi-label">Recibidas Hoy</span>
                                </div>
                            </div>
                            <!-- F23: Avg Response Time KPI -->
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(139,92,246,0.15); color: #8B5CF6"><i class="fa fa-clock-o"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value"><t t-esc="state.dashboardData.avg_response_time"/> min</span>
                                    <span class="kpi-label">Tiempo Medio Respuesta</span>
                                </div>
                            </div>
                            <div class="kpi-card">
                                <div class="kpi-icon" style="background: rgba(6,182,212,0.15); color: #06B6D4"><i class="fa fa-envelope"/></div>
                                <div class="kpi-data">
                                    <span class="kpi-value" t-esc="state.dashboardData.total_messages_7d"/>
                                    <span class="kpi-label">Mensajes (7d)</span>
                                </div>
                            </div>
                        </div>

                        <!-- Activity Chart (CSS bars) -->
                        <div t-if="state.dashboardData.activity and state.dashboardData.activity.length" class="chart-section">
                            <h4>📈 Actividad (7 Días)</h4>
                            <t t-set="maxActivity" t-value="Math.max(1, ...state.dashboardData.activity.map(d => d.received + d.sent))"/>
                            <div class="bar-chart">
                                <t t-foreach="state.dashboardData.activity" t-as="day" t-key="day.day">
                                    <div class="bar-group">
                                        <div class="bar-stack">
                                            <div class="bar bar-received" t-attf-style="height: {{ Math.min((day.received / maxActivity) * 100, 100) }}%" t-att-title="'Recibidas: ' + day.received"/>
                                            <div class="bar bar-sent" t-attf-style="height: {{ Math.min((day.sent / maxActivity) * 100, 100) }}%" t-att-title="'Enviadas: ' + day.sent"/>
                                        </div>
                                        <span class="bar-label" t-esc="day.day.slice(5)"/>
                                    </div>
                                </t>
                            </div>
                            <div class="chart-legend">
                                <span><span class="legend-dot" style="background: #3B82F6"/> Recibidas</span>
                                <span><span class="legend-dot" style="background: #10B981"/> Enviadas</span>
                            </div>
                        </div>

                        <!-- F21: Activity Heatmap -->
                        <div t-if="state.dashboardData.heatmap" class="chart-section">
                            <h4>🗓️ Mapa de Calor de Actividad (7 días)</h4>
                            <div class="heatmap-container">
                                <div class="heatmap-row heatmap-header">
                                    <span class="heatmap-day-label"/>
                                    <t t-foreach="Array.from({length: 24}, (_, i) => i)" t-as="h" t-key="h">
                                        <span t-if="h % 3 === 0" class="heatmap-hour-label" t-esc="h + 'h'"/>
                                        <span t-else="" class="heatmap-hour-label"/>
                                    </t>
                                </div>
                                <t t-set="dayNames" t-value="['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb']"/>
                                <t t-foreach="[1,2,3,4,5,6,0]" t-as="dayIdx" t-key="dayIdx">
                                    <div t-if="state.dashboardData.heatmap[dayIdx]" class="heatmap-row">
                                        <span class="heatmap-day-label" t-esc="dayNames[dayIdx]"/>
                                        <t t-foreach="state.dashboardData.heatmap[dayIdx]" t-as="count" t-key="count_index">
                                            <div class="heatmap-cell" 
                                                 t-attf-style="opacity: {{ count > 0 ? Math.max(0.15, count / (state.dashboardData.heatmapMax || 1)) : 0.04 }}"
                                                 t-att-title="count + ' mensajes'"/>
                                        </t>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <!-- F20: Top Agents Performance -->
                        <div t-if="state.dashboardData.top_agents and state.dashboardData.top_agents.length" class="agents-section">
                            <h4>🏆 Rendimiento Agentes (7 días)</h4>
                            <table class="agent-table">
                                <thead>
                                    <tr>
                                        <th>Agente</th>
                                        <th>Enviadas</th>
                                        <th>Resueltas</th>
                                        <th>Volume</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="state.dashboardData.top_agents" t-as="agent" t-key="agent.agent">
                                        <tr>
                                            <td class="agent-name-cell">
                                                <span class="agent-rank" t-esc="agent_index + 1"/>
                                                <span t-esc="agent.agent"/>
                                            </td>
                                            <td t-esc="agent.sent"/>
                                            <td t-esc="agent.resolved"/>
                                            <td>
                                                <div class="agent-bar-mini">
                                                    <div class="agent-bar-fill" t-attf-style="width: {{ (agent.sent / (state.dashboardData.top_agents[0]?.sent || 1)) * 100 }}%"/>
                                                </div>
                                            </td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                        </div>

                        <!-- F23: SLA Alerts -->
                        <div t-if="state.dashboardData.sla_breaches and state.dashboardData.sla_breaches.length" class="sla-section">
                            <h4>⚠️ Alertas SLA (> <t t-esc="state.dashboardData.sla_target"/> min)</h4>
                            <div class="sla-alerts">
                                <t t-foreach="state.dashboardData.sla_breaches" t-as="breach" t-key="breach_index">
                                    <div class="sla-alert-item">
                                        <div class="sla-alert-icon">
                                            <i t-attf-class="fa {{ breach.waitMinutes > 60 ? 'fa-exclamation-circle text-danger' : 'fa-warning text-warning' }}"/>
                                        </div>
                                        <div class="sla-alert-info">
                                            <span class="sla-conv-name" t-esc="breach.convName"/>
                                            <span class="sla-wait-time">
                                                <t t-if="breach.waitMinutes >= 60">
                                                    <t t-esc="Math.floor(breach.waitMinutes / 60)"/>h <t t-esc="breach.waitMinutes % 60"/>m
                                                </t>
                                                <t t-else="">
                                                    <t t-esc="breach.waitMinutes"/> min
                                                </t>
                                            </span>
                                        </div>
                                        <span class="sla-date" t-esc="breach.date ? breach.date.slice(5, 16) : ''"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <!-- F22: Sentiment Analysis -->
                        <div t-if="state.dashboardData.sentiment" class="chart-section">
                            <h4>💬 Análisis de Sentimiento (Mensajes Recibidos)</h4>
                            <div class="sentiment-gauge">
                                <div class="sentiment-score-circle" t-attf-style="--score-color: {{ state.dashboardData.sentiment.score >= 65 ? '#10B981' : state.dashboardData.sentiment.score >= 40 ? '#F59E0B' : '#EF4444' }}">
                                    <span class="sentiment-score-value" t-esc="state.dashboardData.sentiment.score"/>
                                    <span class="sentiment-score-label">/ 100</span>
                                </div>
                                <div class="sentiment-breakdown">
                                    <div class="sentiment-row">
                                        <span class="sentiment-emoji">😊</span>
                                        <span class="sentiment-label">Positivo</span>
                                        <div class="sentiment-bar-wrap">
                                            <div class="sentiment-bar sentiment-pos" t-attf-style="width: {{ state.dashboardData.sentiment.posPercent }}%"/>
                                        </div>
                                        <span class="sentiment-pct" t-esc="state.dashboardData.sentiment.posPercent + '%'"/>
                                    </div>
                                    <div class="sentiment-row">
                                        <span class="sentiment-emoji">😐</span>
                                        <span class="sentiment-label">Neutro</span>
                                        <div class="sentiment-bar-wrap">
                                            <div class="sentiment-bar sentiment-neu" t-attf-style="width: {{ state.dashboardData.sentiment.neuPercent }}%"/>
                                        </div>
                                        <span class="sentiment-pct" t-esc="state.dashboardData.sentiment.neuPercent + '%'"/>
                                    </div>
                                    <div class="sentiment-row">
                                        <span class="sentiment-emoji">😟</span>
                                        <span class="sentiment-label">Negativo</span>
                                        <div class="sentiment-bar-wrap">
                                            <div class="sentiment-bar sentiment-neg" t-attf-style="width: {{ state.dashboardData.sentiment.negPercent }}%"/>
                                        </div>
                                        <span class="sentiment-pct" t-esc="state.dashboardData.sentiment.negPercent + '%'"/>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- CAMPAIGN ANALYTICS -->
                        <div t-if="state.dashboardData.campaigns and state.dashboardData.campaigns.length" class="campaign-analytics-section">
                            <div class="campaign-header">
                                <div class="campaign-title">
                                    <i class="fa fa-crosshairs"/> Resultados por Campaña
                                </div>
                            </div>

                            <!-- Campaign Summary KPIs -->
                            <div class="campaign-kpi-strip">
                                <div class="camp-kpi">
                                    <span class="camp-kpi-value" t-esc="state.dashboardData.campaign_summary.totalTracked"/>
                                    <span class="camp-kpi-label">Contactos Rastreados</span>
                                </div>
                                <div class="camp-kpi-divider"/>
                                <div class="camp-kpi">
                                    <span class="camp-kpi-value" t-esc="state.dashboardData.campaign_summary.totalCampaigns"/>
                                    <span class="camp-kpi-label">Campañas Activas</span>
                                </div>
                                <div class="camp-kpi-divider"/>
                                <div class="camp-kpi" t-if="state.dashboardData.campaign_summary.topCampaign">
                                    <span class="camp-kpi-value">
                                        <t t-esc="state.dashboardData.campaign_summary.topCampaign.conversionRate"/>%
                                    </span>
                                    <span class="camp-kpi-label">Mejor Conversión</span>
                                </div>
                            </div>

                            <!-- Campaign Cards -->
                            <div class="campaign-cards">
                                <t t-foreach="state.dashboardData.campaigns" t-as="camp" t-key="camp.name">
                                    <div class="campaign-card">
                                        <div class="camp-card-header">
                                            <div class="camp-name-row">
                                                <i t-attf-class="fa {{ camp.source === 'meta' ? 'fa-facebook-square' : camp.source === 'google' ? 'fa-google' : 'fa-globe' }}" 
                                                   t-attf-style="color: {{ camp.source === 'meta' ? '#1877F2' : camp.source === 'google' ? '#4285F4' : '#0ea5e9' }}"/>
                                                <span class="camp-name" t-esc="camp.name"/>
                                            </div>
                                            <div class="camp-badges">
                                                <span t-if="camp.source" class="camp-badge source" t-esc="camp.source"/>
                                                <span t-if="camp.medium" class="camp-badge medium" t-esc="camp.medium"/>
                                            </div>
                                        </div>

                                        <div class="camp-metrics-grid">
                                            <!-- Contacts -->
                                            <div class="camp-metric">
                                                <div class="camp-metric-icon contacts">
                                                    <i class="fa fa-users"/>
                                                </div>
                                                <div class="camp-metric-data">
                                                    <span class="camp-metric-value" t-esc="camp.contacts"/>
                                                    <span class="camp-metric-label">Contactos</span>
                                                </div>
                                            </div>

                                            <!-- Messages -->
                                            <div class="camp-metric">
                                                <div class="camp-metric-icon messages">
                                                    <i class="fa fa-comments"/>
                                                </div>
                                                <div class="camp-metric-data">
                                                    <span class="camp-metric-value" t-esc="camp.messages.total"/>
                                                    <span class="camp-metric-label">Mensajes</span>
                                                </div>
                                            </div>

                                            <!-- Conversion -->
                                            <div class="camp-metric">
                                                <div class="camp-metric-icon conversion">
                                                    <i class="fa fa-check-circle"/>
                                                </div>
                                                <div class="camp-metric-data">
                                                    <span class="camp-metric-value"><t t-esc="camp.conversionRate"/>%</span>
                                                    <span class="camp-metric-label">Conversión</span>
                                                </div>
                                            </div>

                                            <!-- Lead Score -->
                                            <div class="camp-metric">
                                                <div class="camp-metric-icon score">
                                                    <i class="fa fa-star"/>
                                                </div>
                                                <div class="camp-metric-data">
                                                    <span class="camp-metric-value" t-esc="camp.avgScore"/>
                                                    <span class="camp-metric-label">Score Medio</span>
                                                </div>
                                            </div>
                                        </div>

                                        <!-- Temperature Distribution Bar -->
                                        <div class="camp-temp-bar">
                                            <div class="camp-temp-label">
                                                <span>Temperatura de los Leads</span>
                                            </div>
                                            <div class="camp-temp-track">
                                                <div t-if="camp.hot > 0" class="camp-temp-segment hot" 
                                                     t-attf-style="width: {{ (camp.hot / camp.contacts) * 100 }}%"
                                                     t-attf-title="🔥 Hot: {{ camp.hot }}">
                                                    <span t-if="(camp.hot / camp.contacts) > 0.15" t-esc="camp.hot"/>
                                                </div>
                                                <div t-if="camp.warm > 0" class="camp-temp-segment warm" 
                                                     t-attf-style="width: {{ (camp.warm / camp.contacts) * 100 }}%"
                                                     t-attf-title="🌤️ Warm: {{ camp.warm }}">
                                                    <span t-if="(camp.warm / camp.contacts) > 0.15" t-esc="camp.warm"/>
                                                </div>
                                                <div t-if="camp.cold > 0" class="camp-temp-segment cold" 
                                                     t-attf-style="width: {{ (camp.cold / camp.contacts) * 100 }}%"
                                                     t-attf-title="🧊 Cold: {{ camp.cold }}">
                                                    <span t-if="(camp.cold / camp.contacts) > 0.15" t-esc="camp.cold"/>
                                                </div>
                                            </div>
                                            <div class="camp-temp-legend">
                                                <span class="legend-item"><span class="dot hot"/>🔥 <t t-esc="camp.hot"/></span>
                                                <span class="legend-item"><span class="dot warm"/>🌤️ <t t-esc="camp.warm"/></span>
                                                <span class="legend-item"><span class="dot cold"/>🧊 <t t-esc="camp.cold"/></span>
                                            </div>
                                        </div>

                                        <!-- Conversion Progress Bar -->
                                        <div class="camp-conversion-bar">
                                            <div class="camp-conv-track">
                                                <div class="camp-conv-fill" t-attf-style="width: {{ camp.conversionRate }}%; background: {{ camp.conversionRate >= 50 ? 'linear-gradient(90deg, #10B981, #059669)' : camp.conversionRate >= 25 ? 'linear-gradient(90deg, #F59E0B, #D97706)' : 'linear-gradient(90deg, #EF4444, #DC2626)' }}"/>
                                            </div>
                                            <span class="camp-conv-label">
                                                <t t-esc="camp.resolved"/>/<t t-esc="camp.contacts"/> convertidos
                                            </span>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <!-- No campaigns message -->
                        <div t-elif="!state.dashboardData.campaigns or !state.dashboardData.campaigns.length" class="campaign-empty">
                            <div class="campaign-empty-icon">
                                <i class="fa fa-crosshairs"/>
                            </div>
                            <p>Sin datos de campañas. Los contactos rastreados con UTM aparecerán aquí.</p>
                        </div>
                    </div>
                    <div t-else="" class="empty-chat">
                        <div class="empty-icon"><i class="fa fa-bar-chart"/></div>
                        <h3>Dashboard</h3>
                        <p>Ningún dato disponible</p>
                    </div>
                </div>

                <!-- ═══ KANBAN VIEW ═══ -->
                <div t-if="state.viewMode === 'kanban'" class="kanban-view-container">
                    <div class="kanban-header">
                        <!-- Manager: User filter -->
                        <div t-if="state.isManager and state.kanbanUsers.length" class="pipeline-selector" style="margin-right: 12px;">
                            <label><i class="fa fa-user"/> Usuário:</label>
                            <select class="form-select form-select-sm" 
                                    t-on-change="onKanbanUserFilterChange">
                                <option value="">Meu Kanban</option>
                                <t t-foreach="state.kanbanUsers" t-as="u" t-key="u.id">
                                    <option t-att-value="u.id" t-att-selected="u.id === state.kanbanFilterUserId">
                                        <t t-esc="u.name"/>
                                    </option>
                                </t>
                            </select>
                        </div>
                        <div class="pipeline-selector">
                            <label>Embudo:</label>
                            <select class="form-select form-select-sm" 
                                    t-on-change="onPipelineSelectChange">
                                <t t-foreach="state.pipelines" t-as="p" t-key="p.id">
                                    <option t-att-value="p.id" t-att-selected="p.id === state.selectedPipelineId">
                                        <t t-esc="p.icon"/> <t t-esc="p.name"/>
                                    </option>
                                </t>
                            </select>
                        </div>
                    </div>

                    <div class="kanban-board">
                        <div t-if="state.loadingKanban" class="text-center p-4 w-100">
                                <i class="fa fa-spinner fa-spin fa-2x"/>
                            </div>
                        <div t-elif="state.kanbanStages.length" class="kanban-stages">
                            <t t-foreach="state.kanbanStages" t-as="stage" t-key="stage.id">
                                <div class="kanban-column"
                                     t-on-dragover="onDragOver"
                                     t-on-dragleave="onDragLeave"
                                     t-on-drop="(ev) => this.onDrop(ev, stage.id)">
                                    <div class="kanban-column-header">
                                        <span class="column-title" t-esc="stage.name"/>
                                        <span class="column-count badge bg-secondary">
                                            <t t-esc="(state.kanbanCards[stage.id] || []).length"/>
                                        </span>
                                    </div>
                                    <div class="kanban-column-body">
                                        <t t-foreach="state.kanbanCards[stage.id] || []" t-as="card" t-key="card.id">
                                            <div class="kanban-card" draggable="true"
                                                 t-on-dragstart="(ev) => this.onDragStart(ev, card)"
                                                 t-on-dragend="onDragEnd"
                                                 t-on-click="() => this.onKanbanCardClick(card)">
                                                <div class="card-header-row">
                                                    <strong t-esc="card.contact_name || 'Unknown'"/>
                                                    <span t-if="card.unread_count > 0" class="badge bg-danger badge-sm">
                                                        <t t-esc="card.unread_count"/>
                                                    </span>
                                                </div>
                                                <div class="card-phone text-muted small">
                                                    📱 <t t-esc="card.phone"/>
                                                </div>
                                                <div t-if="card.last_message" class="card-preview">
                                                    <t t-esc="(card.last_message || '').substring(0, 60)"/>
                                                    <span t-if="(card.last_message || '').length > 60">...</span>
                                                </div>
                                                <div t-if="getCardTags(card).length" class="conv-tags" style="margin: 4px 0;">
                                                    <t t-foreach="getCardTags(card)" t-as="tagId" t-key="tagId">
                                                        <span class="tag-badge-inline" t-attf-style="background: {{ getTagColor(tagId) }}20; color: {{ getTagColor(tagId) }}; border: 1px solid {{ getTagColor(tagId) }}44">
                                                            <t t-esc="getTagName(tagId)"/>
                                                        </span>
                                                    </t>
                                                </div>
                                                <div class="card-footer">
                                                    <span t-if="card.assigned_user_id" class="card-user small text-muted">
                                                        <i class="fa fa-user"/> <t t-esc="card.assigned_user_id[1]"/>
                                                    </span>
                                                </div>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                            </t>
                        </div>
                        <div t-else="" class="empty-chat">
                                <div class="empty-icon">
                                    <i class="fa fa-columns"/>
                                </div>
                                <h3>Sin Embudo Seleccionado</h3>
                                <p>Selecciona o crea un embudo en ⚙️ Configuración</p>
                            </div>
                    </div>
                </div>
            </div>

            <!-- RIGHT PANEL: Contact Details -->
            <div t-if="state.showContactPanel and state.selectedConversation" class="bader-inbox-right">
                    <!-- 360° Customer Profile Header -->
                    <div class="customer-360-header">
                        <div class="c360-profile">
                            <div t-attf-class="c360-avatar {{ state.selectedConversation.profile_pic_url ? '' : getAvatarColor(state.selectedConversation.id) }}">
                                <img t-if="state.selectedConversation.profile_pic_url" t-att-src="state.selectedConversation.profile_pic_url" class="c360-avatar-img" alt=""/>
                                <t t-else="" t-esc="getInitials(state.selectedConversation.computed_name || state.selectedConversation.phone)"/>
                            </div>
                            <div class="c360-info">
                                <div class="c360-name-row">
                                    <h3 t-esc="state.selectedConversation.computed_name || 'Desconocido'"/>
                                    <span class="c360-status-badge"
                                          t-attf-style="background: {{ getCustomerStatusColor(state.customerStats.customerStatus) }}15; color: {{ getCustomerStatusColor(state.customerStats.customerStatus) }}">
                                        <t t-esc="getCustomerStatusLabel(state.customerStats.customerStatus)"/>
                                    </span>
                                </div>
                                <span class="c360-phone"><i class="fa fa-phone"/> <t t-esc="state.selectedConversation.phone"/></span>
                                <div class="c360-meta">
                                    <span t-if="state.selectedConversation.channel_id"><i class="fa fa-whatsapp"/> <t t-esc="state.selectedConversation.channel_id[1]"/></span>
                                    <span t-if="state.selectedConversation.assigned_user_id"><i class="fa fa-user"/> <t t-esc="state.selectedConversation.assigned_user_id[1]"/></span>
                                </div>
                            </div>
                        </div>

                        <!-- Mini KPI Strip -->
                        <div class="c360-kpi-strip">
                            <div class="c360-kpi">
                                <span class="c360-kpi-value"><t t-esc="state.customerStats.totalMessages"/></span>
                                <span class="c360-kpi-label">Msgs</span>
                            </div>
                            <div class="c360-kpi-divider"/>
                            <div class="c360-kpi">
                                <span class="c360-kpi-value">€<t t-esc="formatCurrency(state.customerStats.pipelineValue)"/></span>
                                <span class="c360-kpi-label">Embudo</span>
                            </div>
                            <div class="c360-kpi-divider"/>
                            <div class="c360-kpi">
                                <span class="c360-kpi-value">€<t t-esc="formatCurrency(state.customerStats.lifetimeValue)"/></span>
                                <span class="c360-kpi-label">Lifetime</span>
                            </div>
                        </div>
                    </div>

                    <!-- AI Intelligence Section -->
                    <div t-if="state.selectedConversation.ai_response_count" class="ai-intelligence-section">
                        <h4>🤖 AI Intelligence</h4>
                        <div class="ai-intel-grid">
                            <!-- Lead Score -->
                            <div class="ai-intel-card">
                                <div class="ai-intel-header">
                                    <span class="ai-intel-label">Lead Score</span>
                                    <span t-att-class="'ai-temp-badge ai-temp-' + (state.selectedConversation.ai_lead_temperature or 'cold')">
                                        <t t-if="state.selectedConversation.ai_lead_temperature === 'hot'">🔥 Hot</t>
                                        <t t-elif="state.selectedConversation.ai_lead_temperature === 'warm'">🌤️ Warm</t>
                                        <t t-else="">🧊 Cold</t>
                                    </span>
                                </div>
                                <div class="ai-score-bar">
                                    <div t-att-class="'ai-score-fill ai-score-' + (state.selectedConversation.ai_lead_temperature or 'cold')"
                                         t-att-style="'width: ' + (state.selectedConversation.ai_lead_score or 0) + '%'"/>
                                </div>
                                <span class="ai-score-value"><t t-esc="state.selectedConversation.ai_lead_score or 0"/>/100</span>
                            </div>

                            <!-- Resolution Status -->
                            <div class="ai-intel-card">
                                <div class="ai-intel-header">
                                    <span class="ai-intel-label">Status</span>
                                    <span t-att-class="'ai-resolution-badge ai-res-' + (state.selectedConversation.ai_resolution or 'pending')">
                                        <t t-if="state.selectedConversation.ai_resolution === 'resolved'">✅ Resuelto</t>
                                        <t t-elif="state.selectedConversation.ai_resolution === 'escalated'">⚠️ Escalado</t>
                                        <t t-else="">⏳ Pendiente</t>
                                    </span>
                                </div>
                                <div class="ai-intel-meta">
                                    <span><i class="fa fa-comments-o"/> <t t-esc="state.selectedConversation.ai_response_count or 0"/> respuestas AI</span>
                                </div>
                            </div>
                        </div>

                        <!-- Escalation Reason -->
                        <div t-if="state.selectedConversation.ai_escalation_reason" class="ai-escalation-alert">
                            <i class="fa fa-exclamation-triangle"/> <t t-esc="state.selectedConversation.ai_escalation_reason"/>
                        </div>
                    </div>

                    <!-- Tags Section -->
                    <div class="contact-section">
                        <h4>🏷️ Etiquetas</h4>
                        
                        <!-- Active tags on this conversation (colored pills with ×) -->
                        <div t-if="(state.selectedConversation.tag_ids || []).length" class="tag-active-list">
                            <t t-foreach="state.selectedConversation.tag_ids || []" t-as="tagId" t-key="tagId">
                                <span class="tag-pill" t-attf-style="background: {{ getTagColor(tagId) }}20; color: {{ getTagColor(tagId) }}; border-color: {{ getTagColor(tagId) }}44">
                                    <span class="tag-pill-dot" t-attf-style="background: {{ getTagColor(tagId) }}"/>
                                    <t t-esc="getTagName(tagId)"/>
                                    <button class="tag-pill-remove" t-on-click.stop="() => this.toggleTag(state.selectedConversation.id, tagId)" title="Quitar tag">×</button>
                                </span>
                            </t>
                        </div>
                        <p t-else="" class="text-muted small mb-2" style="font-style:italic; opacity:0.6">Sin tags asignados</p>

                        <!-- Create new tag: color dot + input + button in one row -->
                        <div class="tag-create-inline">
                            <button class="tag-color-cycle" 
                                    t-attf-style="background: {{ tagColors[state.newTagColor].hex }}"
                                    t-on-click="() => { this.state.newTagColor = (this.state.newTagColor + 1) % 12 }"
                                    title="Click para cambiar color"/>
                            <input type="text" class="tag-create-input" 
                                   placeholder="Crear nuevo tag..." 
                                   t-model="state.newTagName"
                                   t-on-keydown="(ev) => ev.key === 'Enter' and this.createTag()"/>
                            <button class="tag-create-btn" t-on-click="() => this.createTag()" title="Crear tag">
                                <i class="fa fa-plus"/>
                            </button>
                        </div>

                        <!-- Available tags (not yet assigned) for quick add -->
                        <div t-if="state.allTags.length" class="tag-available">
                            <span class="tag-available-label">Disponibles:</span>
                            <t t-foreach="state.allTags" t-as="tag" t-key="tag.id">
                                <button t-if="!(state.selectedConversation.tag_ids || []).includes(tag.id)"
                                        class="tag-outline-btn"
                                        t-on-click="() => this.toggleTag(state.selectedConversation.id, tag.id)"
                                        t-attf-style="border-color: {{ getTagColor(tag.id) }}66; color: {{ getTagColor(tag.id) }}">
                                    + <t t-esc="tag.name"/>
                                </button>
                            </t>
                            <!-- Delete tags option -->
                            <button class="tag-manage-btn" t-on-click="() => { this.state.showTagManager = !this.state.showTagManager }" title="Gestionar tags">
                                <i class="fa fa-cog"/>
                            </button>
                        </div>

                        <!-- Tag management (delete) panel -->
                        <div t-if="state.showTagManager" class="tag-manage-panel">
                            <t t-foreach="state.allTags" t-as="tag" t-key="tag.id">
                                <div class="tag-manage-item">
                                    <span class="tag-manage-dot" t-attf-style="background: {{ getTagColor(tag.id) }}"/>
                                    <span class="tag-manage-name" t-esc="tag.name"/>
                                    <button class="tag-manage-delete" t-on-click="() => this.deleteTag(tag.id)" title="Eliminar tag permanentemente">
                                        <i class="fa fa-trash"/>
                                    </button>
                                </div>
                            </t>
                        </div>
                    </div>

                    <!-- Tabs: Fluxos / Notes / Scheduled / CRM -->
                    <div class="contact-section">
                        <div class="panel-tabs">
                            <button t-attf-class="panel-tab {{ state.notesTab === 'info' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('info')">
                                📊 Flujos
                            </button>
                            <button t-attf-class="panel-tab {{ state.notesTab === 'notes' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('notes')">
                                📝 Notas
                            </button>
                            <button t-attf-class="panel-tab {{ state.notesTab === 'scheduled' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('scheduled')">
                                ⏰ Program.
                            </button>
                            <button t-attf-class="panel-tab {{ state.notesTab === 'crm' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('crm')">
                                💼 CRM
                            </button>
                            <button t-attf-class="panel-tab {{ state.notesTab === 'sales' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('sales')">
                                📦 Ventas
                            </button>
                            <button t-attf-class="panel-tab {{ state.notesTab === 'activities' ? 'active' : '' }}"
                                    t-on-click="() => this.setNotesTab('activities')">
                                📅 Activid.
                                <span t-if="state.contactActivities.length" class="badge bg-danger ms-1" style="font-size: 8px; padding: 2px 4px;" t-esc="state.contactActivities.length"/>
                            </button>
                        </div>

                        <!-- Info Tab (Embudos) -->
                        <div t-if="state.notesTab === 'info'">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="small text-muted">Embudos</span>
                                <button class="btn btn-sm btn-outline-primary" t-on-click="openAddToPipelineModal" 
                                        t-att-disabled="!availablePipelinesForModal.length"
                                        title="Añadir a un embudo">
                                    <i class="fa fa-plus"/>
                                </button>
                            </div>
                            <div t-if="state.loadingPipelines" class="text-center py-2">
                                <i class="fa fa-spinner fa-spin"/>
                            </div>
                            <div t-elif="state.convPipelines.length" class="pipelines-list">
                                <t t-foreach="state.convPipelines" t-as="cp" t-key="cp.id">
                                    <div class="pipeline-assignment-row">
                                        <div class="pipeline-info">
                                            <strong t-esc="cp.pipeline_id[1]"/>
                                            <span class="badge bg-info ms-1" t-esc="cp.stage_id[1]"/>
                                        </div>
                                        <button class="btn btn-sm btn-link text-danger p-0" 
                                                t-on-click="() => this.removeFromPipeline(cp.id)" title="Eliminar">
                                            <i class="fa fa-times"/>
                                        </button>
                                    </div>
                                </t>
                            </div>
                            <div t-else="" class="no-pipelines">
                                <p class="text-muted small mb-0">Ningún flujo asignado</p>
                            </div>
                        </div>

                        <!-- Notes Tab -->
                        <div t-if="state.notesTab === 'notes'" class="notes-section">
                            <div class="note-composer">
                                <textarea rows="2" class="form-control form-control-sm" 
                                          placeholder="Añadir nota interna..."
                                          t-model="state.noteText"/>
                                <button class="btn btn-sm btn-primary mt-1" t-on-click="addNote"
                                        t-att-disabled="!state.noteText.trim()">
                                    <i class="fa fa-plus"/> Nota
                                </button>
                            </div>
                            <div t-if="state.loadingNotes" class="text-center py-3">
                                <i class="fa fa-spinner fa-spin"/>
                            </div>
                            <div t-elif="state.notes.length" class="notes-list">
                                <t t-foreach="state.notes" t-as="note" t-key="note.id">
                                    <div class="note-card">
                                        <div class="note-header">
                                            <span class="note-author" t-esc="note.author_id?.[1] || 'Sistema'"/>
                                            <span class="note-date" t-esc="formatTime(note.create_date)"/>
                                            <button class="btn-note-delete" t-on-click="() => this.deleteNote(note.id)" title="Eliminar">
                                                <i class="fa fa-trash"/>
                                            </button>
                                        </div>
                                        <p class="note-content" t-esc="note.content"/>
                                    </div>
                                </t>
                            </div>
                            <div t-else="" class="text-center py-3">
                                <p class="text-muted small">Sin notas internas</p>
                            </div>
                        </div>

                        <!-- Scheduled Tab -->
                        <div t-if="state.notesTab === 'scheduled'" class="scheduled-section">
                            <div t-if="state.scheduledMessages.length" class="scheduled-list">
                                <t t-foreach="state.scheduledMessages" t-as="sm" t-key="sm.id">
                                    <div class="scheduled-card">
                                        <div class="scheduled-info">
                                            <span class="scheduled-time">
                                                <i class="fa fa-clock-o"/> <t t-esc="formatTime(sm.scheduled_datetime)"/>
                                            </span>
                                            <p class="scheduled-content" t-esc="sm.content"/>
                                        </div>
                                        <button class="btn btn-sm btn-link text-danger" 
                                                t-on-click="() => this.cancelScheduledMessage(sm.id)" title="Cancelar">
                                            <i class="fa fa-times"/>
                                        </button>
                                    </div>
                                </t>
                            </div>
                            <div t-else="" class="text-center py-3">
                                <p class="text-muted small">Sin mensajes programados</p>
                            </div>
                        </div>

                        <!-- CRM Tab -->
                        <div t-if="state.notesTab === 'crm'" class="crm-section">
                            <!-- Contact Mini Card -->
                            <div class="crm-contact-card" t-on-click="viewPartner">
                                <div class="crm-contact-icon">
                                    <i class="fa fa-user"/>
                                </div>
                                <div class="crm-contact-info">
                                    <span class="crm-contact-name" t-esc="state.selectedConversation?.computed_name || 'Sin nombre'"/>
                                    <span class="crm-contact-detail" t-esc="state.selectedConversation?.phone || ''"/>
                                </div>
                                <div class="crm-contact-action">
                                    <span t-if="state.selectedConversation?.partner_id" class="crm-badge crm-badge-linked">
                                        <i class="fa fa-link"/> Vinculado
                                    </span>
                                    <span t-else="" class="crm-badge crm-badge-new">
                                        <i class="fa fa-plus"/> Crear
                                    </span>
                                </div>
                            </div>

                            <!-- Opportunities Header -->
                            <div class="crm-opp-header">
                                <span class="crm-opp-title">
                                    <i class="fa fa-line-chart"/> Oportunidades
                                    <span t-if="state.contactOpportunities.length" class="crm-opp-count" t-esc="state.contactOpportunities.length"/>
                                </span>
                                <button class="crm-opp-add-btn" t-on-click="createOpportunity" title="Crear Oportunidad">
                                    <i class="fa fa-plus"/>
                                </button>
                            </div>

                            <!-- Loading -->
                            <div t-if="state.loadingOpportunities" class="text-center py-3">
                                <i class="fa fa-spinner fa-spin" style="color: #8B5CF6;"/>
                            </div>

                            <!-- Opportunity Cards -->
                            <div t-elif="state.contactOpportunities.length" class="crm-opp-list">
                                <t t-foreach="state.contactOpportunities" t-as="opp" t-key="opp.id">
                                    <div class="crm-opp-card" t-on-click="() => this.openOpportunity(opp.id)"
                                         t-attf-style="--opp-color: {{ getOppStageColor(opp.stage_id?.[1] || '') }}">
                                        <div class="crm-opp-card-top">
                                            <span class="crm-opp-name" t-esc="opp.name"/>
                                            <span class="crm-opp-stage" t-attf-style="background: {{ getOppStageColor(opp.stage_id?.[1] || '') }}15; color: {{ getOppStageColor(opp.stage_id?.[1] || '') }}">
                                                <t t-esc="opp.stage_id?.[1] || '—'"/>
                                            </span>
                                        </div>
                                        <div class="crm-opp-card-body">
                                            <div class="crm-opp-metric">
                                                <i class="fa fa-euro"/>
                                                <span t-esc="formatCurrency(opp.expected_revenue)"/>
                                            </div>
                                            <div t-if="opp.probability" class="crm-opp-metric">
                                                <i class="fa fa-percent"/>
                                                <span><t t-esc="opp.probability"/>%</span>
                                            </div>
                                            <div t-if="opp.user_id" class="crm-opp-metric">
                                                <i class="fa fa-user-o"/>
                                                <span t-esc="opp.user_id[1]"/>
                                            </div>
                                        </div>
                                        <div class="crm-opp-card-footer">
                                            <span class="crm-opp-date">
                                                <i class="fa fa-calendar-o"/> <t t-esc="formatTime(opp.create_date)"/>
                                            </span>
                                            <span class="crm-opp-type-badge" t-esc="opp.type === 'opportunity' ? 'Oportunidad' : 'Lead'"/>
                                        </div>
                                    </div>
                                </t>
                            </div>

                            <!-- Empty State -->
                            <div t-else="" class="crm-empty-state">
                                <div class="crm-empty-icon">
                                    <i class="fa fa-handshake-o"/>
                                </div>
                                <p>Sin oportunidades</p>
                                <button class="crm-empty-btn" t-on-click="createOpportunity">
                                    <i class="fa fa-plus"/> Crear primera oportunidad
                                </button>
                            </div>
                        </div>

                        <!-- Sales Tab -->
                        <div t-if="state.notesTab === 'sales'" class="sales-section">
                            <!-- Summary Dashboard -->
                            <div class="sales-summary">
                                <div class="sales-summary-card sales-summary-quoted">
                                    <div class="sales-summary-icon"><i class="fa fa-file-text-o"/></div>
                                    <div class="sales-summary-data">
                                        <span class="sales-summary-value">€ <t t-esc="formatCurrency(state.salesTotalQuoted)"/></span>
                                        <span class="sales-summary-label">Presupuestos (<t t-esc="state.contactQuotations.length"/>)</span>
                                    </div>
                                </div>
                                <div class="sales-summary-card sales-summary-ordered">
                                    <div class="sales-summary-icon"><i class="fa fa-check-circle"/></div>
                                    <div class="sales-summary-data">
                                        <span class="sales-summary-value">€ <t t-esc="formatCurrency(state.salesTotalOrdered)"/></span>
                                        <span class="sales-summary-label">Pedidos (<t t-esc="state.contactOrders.length"/>)</span>
                                    </div>
                                </div>
                            </div>

                            <!-- Loading -->
                            <div t-if="state.loadingSales" class="text-center py-3">
                                <i class="fa fa-spinner fa-spin" style="color: #F59E0B;"/>
                            </div>

                            <div t-elif="state.contactQuotations.length || state.contactOrders.length" class="sales-lists">
                                <!-- Quotations Section -->
                                <div t-if="state.contactQuotations.length" class="sales-group">
                                    <div class="sales-group-header">
                                        <span><i class="fa fa-file-text-o"/> Presupuestos Abiertos</span>
                                        <button class="crm-opp-add-btn" t-on-click="createQuotation" title="Nuevo Presupuesto">
                                            <i class="fa fa-plus"/>
                                        </button>
                                    </div>
                                    <t t-foreach="state.contactQuotations" t-as="q" t-key="q.id">
                                        <div class="sales-card" t-on-click="() => this.openSaleOrder(q.id)"
                                             t-attf-style="--sales-color: {{ getOrderStateColor(q.state) }}">
                                            <div class="sales-card-top">
                                                <span class="sales-card-name" t-esc="q.name"/>
                                                <span class="sales-card-state" t-attf-style="background: {{ getOrderStateColor(q.state) }}15; color: {{ getOrderStateColor(q.state) }}">
                                                    <t t-esc="getOrderStateLabel(q.state)"/>
                                                </span>
                                            </div>
                                            <div class="sales-card-body">
                                                <div class="sales-card-amount">
                                                    <span class="sales-card-amount-value">€ <t t-esc="formatCurrency(q.amount_total)"/></span>
                                                </div>
                                                <div class="sales-card-meta">
                                                    <span t-if="q.user_id"><i class="fa fa-user-o"/> <t t-esc="q.user_id[1]"/></span>
                                                    <span><i class="fa fa-calendar-o"/> <t t-esc="formatTime(q.create_date)"/></span>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>

                                <!-- Orders Section -->
                                <div t-if="state.contactOrders.length" class="sales-group">
                                    <div class="sales-group-header">
                                        <span><i class="fa fa-check-circle"/> Pedidos Confirmados</span>
                                    </div>
                                    <t t-foreach="state.contactOrders" t-as="o" t-key="o.id">
                                        <div class="sales-card" t-on-click="() => this.openSaleOrder(o.id)"
                                             t-attf-style="--sales-color: {{ getOrderStateColor(o.state) }}">
                                            <div class="sales-card-top">
                                                <span class="sales-card-name" t-esc="o.name"/>
                                                <span class="sales-card-state" t-attf-style="background: {{ getOrderStateColor(o.state) }}15; color: {{ getOrderStateColor(o.state) }}">
                                                    <t t-esc="getOrderStateLabel(o.state)"/>
                                                </span>
                                            </div>
                                            <div class="sales-card-body">
                                                <div class="sales-card-amount">
                                                    <span class="sales-card-amount-value">€ <t t-esc="formatCurrency(o.amount_total)"/></span>
                                                </div>
                                                <div class="sales-card-meta">
                                                    <span t-if="o.user_id"><i class="fa fa-user-o"/> <t t-esc="o.user_id[1]"/></span>
                                                    <span t-if="o.date_order"><i class="fa fa-calendar-o"/> <t t-esc="formatTime(o.date_order)"/></span>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </div>

                            <!-- Empty State -->
                            <div t-else="" class="crm-empty-state">
                                <div class="crm-empty-icon" style="background: linear-gradient(135deg, rgba(245,158,11,0.1), rgba(16,185,129,0.1));">
                                    <i class="fa fa-shopping-cart" style="color: #F59E0B;"/>
                                </div>
                                <p>Sin presupuestos ni pedidos</p>
                                <button class="crm-empty-btn" t-on-click="createQuotation">
                                    <i class="fa fa-plus"/> Crear presupuesto
                                </button>
                            </div>
                        </div>
                    </div>

                        <!-- Activities Tab -->
                        <div t-if="state.notesTab === 'activities'" class="activities-section">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="small text-muted">Actividades CRM</span>
                                <button class="btn btn-sm btn-outline-primary" t-on-click="createOdooTask"
                                        title="Nueva Actividad">
                                    <i class="fa fa-plus"/>
                                </button>
                            </div>

                            <div t-if="state.loadingActivities" class="text-center py-3">
                                <i class="fa fa-spinner fa-spin"/>
                            </div>

                            <div t-elif="state.contactActivities.length">
                                <!-- Overdue Activities -->
                                <t t-set="overdueActivities" t-value="state.contactActivities.filter(a => a._status === 'overdue')"/>
                                <div t-if="overdueActivities.length" class="mb-2">
                                    <span class="small fw-bold" style="color: #EF4444;">⚠️ Vencidas (<t t-esc="overdueActivities.length"/>)</span>
                                </div>
                                <t t-foreach="overdueActivities" t-as="act" t-key="act.id">
                                    <div class="activity-item activity-overdue">
                                        <div class="activity-icon">
                                            <i class="fa fa-exclamation-circle"/>
                                        </div>
                                        <div class="activity-content" style="cursor: pointer;" t-on-click="() => this.openActivity(act.id)">
                                            <span class="activity-title" t-esc="act.summary || act.activity_type_id?.[1] || 'Actividad'"/>
                                            <span class="activity-type" t-esc="act.activity_type_id?.[1]"/>
                                            <span class="activity-date date-overdue">
                                                <i class="fa fa-clock-o"/> <t t-esc="act.date_deadline"/> - Vencida
                                            </span>
                                        </div>
                                        <button class="activity-done-btn" t-on-click="() => this.markActivityDone(act.id)">
                                            <i class="fa fa-check"/> Hecho
                                        </button>
                                    </div>
                                </t>

                                <!-- Today Activities -->
                                <t t-set="todayActivities" t-value="state.contactActivities.filter(a => a._status === 'today')"/>
                                <div t-if="todayActivities.length" class="mb-2 mt-2">
                                    <span class="small fw-bold" style="color: #F59E0B;">📅 Hoy (<t t-esc="todayActivities.length"/>)</span>
                                </div>
                                <t t-foreach="todayActivities" t-as="act" t-key="act.id">
                                    <div class="activity-item activity-today">
                                        <div class="activity-icon">
                                            <i class="fa fa-clock-o"/>
                                        </div>
                                        <div class="activity-content" style="cursor: pointer;" t-on-click="() => this.openActivity(act.id)">
                                            <span class="activity-title" t-esc="act.summary || act.activity_type_id?.[1] || 'Actividad'"/>
                                            <span class="activity-type" t-esc="act.activity_type_id?.[1]"/>
                                            <span class="activity-date date-today">
                                                <i class="fa fa-clock-o"/> Hoy
                                            </span>
                                        </div>
                                        <button class="activity-done-btn" t-on-click="() => this.markActivityDone(act.id)">
                                            <i class="fa fa-check"/> Hecho
                                        </button>
                                    </div>
                                </t>

                                <!-- Future Activities -->
                                <t t-set="futureActivities" t-value="state.contactActivities.filter(a => a._status === 'planned')"/>
                                <div t-if="futureActivities.length" class="mb-2 mt-2">
                                    <span class="small fw-bold" style="color: #3B82F6;">📋 Próximas (<t t-esc="futureActivities.length"/>)</span>
                                </div>
                                <t t-foreach="futureActivities" t-as="act" t-key="act.id">
                                    <div class="activity-item">
                                        <div class="activity-icon">
                                            <i class="fa fa-calendar"/>
                                        </div>
                                        <div class="activity-content" style="cursor: pointer;" t-on-click="() => this.openActivity(act.id)">
                                            <span class="activity-title" t-esc="act.summary || act.activity_type_id?.[1] || 'Actividad'"/>
                                            <span class="activity-type" t-esc="act.activity_type_id?.[1]"/>
                                            <span class="activity-date date-future">
                                                <i class="fa fa-calendar-o"/> <t t-esc="act.date_deadline"/>
                                            </span>
                                        </div>
                                        <button class="activity-done-btn" t-on-click="() => this.markActivityDone(act.id)">
                                            <i class="fa fa-check"/> Hecho
                                        </button>
                                    </div>
                                </t>
                            </div>

                            <div t-else="" class="text-center py-3">
                                <p class="text-muted small">Sin actividades pendientes</p>
                                <button class="crm-empty-btn" t-on-click="createOdooTask">
                                    <i class="fa fa-plus"/> Crear actividad
                                </button>
                            </div>
                        </div>
                    <!-- Quick Actions Bar -->
                    <div class="quick-actions-bar">
                        <button class="qa-btn qa-btn-quotation" t-on-click="createQuotation" title="Nuevo Presupuesto">
                            <i class="fa fa-file-text-o"/>
                            <span>Presupuesto</span>
                        </button>
                        <button class="qa-btn qa-btn-opportunity" t-on-click="createOpportunity" title="Nueva Oportunidad">
                            <i class="fa fa-handshake-o"/>
                            <span>Oportunidad</span>
                        </button>
                        <button class="qa-btn qa-btn-task" t-on-click="createOdooTask" title="Crear Actividad">
                            <i class="fa fa-calendar-check-o"/>
                            <span>Actividad</span>
                        </button>
                        <button class="qa-btn qa-btn-contact" t-on-click="viewPartner" title="Ver Contacto">
                            <i class="fa fa-address-card-o"/>
                            <span>Contacto</span>
                        </button>
                    </div>

                    <!-- Contact Origin / UTM Tracking -->
                    <div t-if="state.selectedConversation.utm_source or state.selectedConversation.utm_campaign or state.selectedConversation.channel_origin" class="origin-tracking-card">
                        <div class="origin-header">
                            <i class="fa fa-crosshairs"/>
                            <span>Origen del Contacto</span>
                        </div>
                        <div class="origin-grid">
                            <div t-if="state.selectedConversation.utm_source" class="origin-item">
                                <div class="origin-icon source">
                                    <i t-attf-class="fa {{ state.selectedConversation.utm_source === 'meta' ? 'fa-facebook' : state.selectedConversation.utm_source === 'google' ? 'fa-google' : 'fa-globe' }}"/>
                                </div>
                                <div class="origin-detail">
                                    <span class="origin-label">Source</span>
                                    <span class="origin-value" t-esc="state.selectedConversation.utm_source"/>
                                </div>
                            </div>
                            <div t-if="state.selectedConversation.utm_medium" class="origin-item">
                                <div class="origin-icon medium">
                                    <i t-attf-class="fa {{ state.selectedConversation.utm_medium === 'instagram' ? 'fa-instagram' : state.selectedConversation.utm_medium === 'facebook' ? 'fa-facebook' : state.selectedConversation.utm_medium === 'cpc' ? 'fa-money' : 'fa-share-alt' }}"/>
                                </div>
                                <div class="origin-detail">
                                    <span class="origin-label">Medium</span>
                                    <span class="origin-value" t-esc="state.selectedConversation.utm_medium"/>
                                </div>
                            </div>
                            <div t-if="state.selectedConversation.utm_campaign" class="origin-item full-width">
                                <div class="origin-icon campaign">
                                    <i class="fa fa-bullhorn"/>
                                </div>
                                <div class="origin-detail">
                                    <span class="origin-label">Campaña</span>
                                    <span class="origin-value" t-esc="state.selectedConversation.utm_campaign"/>
                                </div>
                            </div>
                            <div t-if="state.selectedConversation.channel_origin" class="origin-item">
                                <div class="origin-icon channel">
                                    <i class="fa fa-plug"/>
                                </div>
                                <div class="origin-detail">
                                    <span class="origin-label">Canal</span>
                                    <span class="origin-value" t-esc="state.selectedConversation.channel_origin"/>
                                </div>
                            </div>
                        </div>
                        <div t-if="state.selectedConversation.tracked_at" class="origin-footer">
                            <i class="fa fa-clock-o"/>
                            <span t-esc="formatTime(state.selectedConversation.tracked_at)"/>
                        </div>
                    </div>
                </div>
        </div>

        <!-- ADD CHANNEL MODAL -->
        <div t-if="state.showChannelModal" class="channel-modal-wrapper">
            <div class="bader-modal-overlay" t-on-click="closeChannelModal">
                <div class="bader-modal" t-on-click.stop="">
                    <div class="bader-modal-header">
                        <h4><i class="fa fa-whatsapp"/> Conectar WhatsApp</h4>
                        <button class="btn-close" t-on-click="closeChannelModal"/>
                    </div>
                    <div class="bader-modal-body">
                        <!-- Step 1: Enter Name -->
                        <div t-if="state.channelStep === 1" class="channel-step-1">
                            <div class="mb-3">
                                <label class="form-label">Nombre del Canal</label>
                                <input type="text" class="form-control form-control-lg" 
                                    placeholder="Ex: Bader WhatsApp" 
                                    t-model="state.newChannelName"/>
                            </div>
                            <button class="btn btn-success btn-lg w-100" 
                                t-on-click="createChannel" 
                                t-att-disabled="!state.newChannelName.trim() || state.creatingChannel">
                                <span t-if="state.creatingChannel">
                                    <i class="fa fa-spinner fa-spin"/> Creando...
                                </span>
                                <span t-else="">
                                    <i class="fa fa-arrow-right"/> Continuar
                                </span>
                            </button>
                        </div>
                        
                        <!-- Step 2: Show QR Code -->
                        <div t-if="state.channelStep === 2" class="channel-step-2">
                            <div class="text-center">
                                <p class="mb-3">Escanee el código QR con WhatsApp</p>
                                <div t-if="state.qrCodeData" class="qr-data">
                                    <div class="qr-container">
                                        <img t-att-src="'data:image/png;base64,' + state.qrCodeData" 
                                            class="qr-code-img" alt="QR Code"/>
                                    </div>
                                    <p class="small text-muted mt-3">
                                        <i class="fa fa-mobile"/> Abra WhatsApp &gt; Dispositivos Vinculados &gt; Vincular Dispositivo
                                    </p>
                                </div>
                                <div t-else="" class="qr-loading">
                                    <i class="fa fa-spinner fa-spin fa-3x"/>
                                    <p class="mt-2">Obteniendo código QR...</p>
                                </div>
                                <button class="btn btn-outline-secondary mt-3" t-on-click="refreshQRCode">
                                    <i class="fa fa-refresh"/> Actualizar código QR
                                </button>
                            </div>
                        </div>
                        
                        <!-- Step 3: Connected -->
                        <div t-if="state.channelStep === 3" class="channel-step-3">
                            <div class="text-center">
                                <div class="success-icon">
                                    <i class="fa fa-check-circle text-success"/>
                                </div>
                                <h4 class="mt-3">¡Conectado!</h4>
                                <p class="text-muted" t-esc="state.connectedPhone"/>
                                <button class="btn btn-success btn-lg w-100 mt-3" t-on-click="closeChannelModal">
                                    <i class="fa fa-check"/> Completado
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ADD TO PIPELINE MODAL -->
        <div t-if="state.showPipelineModal" class="pipeline-modal-wrapper">
            <div class="bader-modal-overlay" t-on-click="closePipelineModal">
                <div class="bader-modal" t-on-click.stop="" style="max-width: 400px;">
                    <div class="bader-modal-header">
                        <h4><i class="fa fa-columns"/> Añadir a Embudo</h4>
                        <button class="btn-close" t-on-click="closePipelineModal"/>
                    </div>
                    <div class="bader-modal-body">
                        <div t-if="availablePipelinesForModal.length" class="pipeline-select-form">
                            <div class="mb-3">
                                <label class="form-label">Selecciona el pipeline:</label>
                                <select class="form-select" 
                                        t-on-change="onPipelineModalChange">
                                    <option value="">-- Elegir --</option>
                                    <t t-foreach="availablePipelinesForModal" t-as="p" t-key="p.id">
                                        <option t-att-value="p.id">
                                            <t t-esc="p.icon"/> <t t-esc="p.name"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <button class="btn btn-primary w-100" t-on-click="addToPipeline"
                                    t-att-disabled="!state.pipelineModalPipelineId">
                                <i class="fa fa-plus"/> Añadir
                            </button>
                        </div>
                        <div t-else="" class="no-pipelines-msg">
                            <p class="text-muted">Esta conversación ya está en todos los pipelines disponibles.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- NEW CONVERSATION MODAL -->
        <div t-if="state.showNewConversationModal" class="new-conv-modal-wrapper">
            <div class="bader-modal-overlay" t-on-click="closeNewConversationModal">
                <div class="bader-modal new-conv-modal" t-on-click.stop="">
                    <div class="bader-modal-header">
                        <h4><i class="fa fa-comment"/> Nueva Conversación</h4>
                        <button class="btn-close" t-on-click="closeNewConversationModal"/>
                    </div>
                    <div class="bader-modal-body">
                        <!-- Channel Selector (if multiple) -->
                        <div t-if="state.channels.length > 1" class="new-conv-channel-select mb-3">
                            <label class="form-label small text-muted">Canal WhatsApp:</label>
                            <div class="d-flex gap-2 flex-wrap">
                                <t t-foreach="state.channels" t-as="ch" t-key="ch.id">
                                    <button t-attf-class="btn btn-sm {{ state.selectedChannelId === ch.id ? 'btn-success' : 'btn-outline-secondary' }}"
                                            t-on-click="() => this.state.selectedChannelId = ch.id">
                                        <i class="fa fa-whatsapp"/> <t t-esc="ch.name"/>
                                    </button>
                                </t>
                            </div>
                        </div>

                        <!-- Search Input -->
                        <div class="new-conv-search-wrapper">
                            <i class="fa fa-search new-conv-search-icon"/>
                            <input type="text" class="form-control new-conv-search-input"
                                   placeholder="Buscar contacto por nombre, teléfono o email..."
                                   t-att-value="state.contactSearchQuery"
                                   t-on-input="onContactSearchInput"
                                   autofocus="1"/>
                            <span t-if="state.searchingContacts" class="search-spinner-wrap">
                                <i class="fa fa-spinner fa-spin new-conv-search-spinner"/>
                            </span>
                        </div>

                        <!-- Results -->
                        <div class="new-conv-results">
                            <div t-if="state.searchingContacts and !state.contactSearchResults.length" class="new-conv-empty">
                                <i class="fa fa-spinner fa-spin fa-2x"/>
                                <p>Buscando...</p>
                            </div>
                            <div t-elif="!state.contactSearchQuery" class="new-conv-empty">
                                <i class="fa fa-address-book fa-3x"/>
                                <p>Busca un contacto de Odoo</p>
                                <span class="text-muted small">Busca por nombre, teléfono o email</span>
                            </div>
                            <div t-elif="state.contactSearchQuery and !state.searchingContacts and !state.contactSearchResults.length" class="new-conv-empty">
                                <i class="fa fa-user-times fa-2x"/>
                                <p>Ningún contacto encontrado</p>
                                <span class="text-muted small">Intenta con otro nombre o número</span>
                            </div>
                            <div t-else="" class="contact-search-results">
                                <t t-foreach="state.contactSearchResults" t-as="contact" t-key="contact.id">
                                    <div class="new-conv-contact" t-on-click="() => this.startConversation(contact)">
                                        <div class="new-conv-contact-avatar">
                                            <img t-if="contact.has_image" t-attf-src="/web/image/res.partner/{{ contact.id }}/image_128" alt="Avatar"/>
                                            <span t-else="" t-attf-class="avatar-initials {{ getAvatarColor(contact.id) }}">
                                                <t t-esc="getContactInitials(contact.name)"/>
                                            </span>
                                        </div>
                                        <div class="new-conv-contact-info">
                                            <div class="new-conv-contact-name" t-esc="contact.name"/>
                                            <div class="new-conv-contact-details">
                                                <span class="new-conv-phone">
                                                    <i class="fa fa-phone"/> <t t-esc="contact.display_phone"/>
                                                </span>
                                                <span t-if="contact.company_name" class="new-conv-company">
                                                    <i class="fa fa-building-o"/> <t t-esc="contact.company_name"/>
                                                </span>
                                            </div>
                                        </div>
                                        <div class="new-conv-contact-action">
                                            <i class="fa fa-whatsapp"/>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
